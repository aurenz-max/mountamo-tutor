# Reader-fit 14m — THE SWEEP: canonical-first grade resolution across 20 generators — 2026-08-04

**Outcome: 14m is CLOSED. Every generator-local grade resolver named by the
census — 8 K-2/elementary + hundreds-chart (14i) + coin-counter (14c) + 11
chemistry — now resolves its band canonical-first (`ctx.grade` via an exported
per-generator mapper), with the legacy prose/default parser kept as the
fallback, never deleted. Plus one generator the census under-counted
(matter-explorer, inline resolver). 20 generators edited in one day-slice,
all machine-gated: 43 new tests across 7 suites (revert-bite proven on every
wired generator), typecheck:lumina 0, full tsc 803 = baseline, full vitest
1400/1400, and 21 real-Gemini `/eval-test` probes through the production
route.**

Template per generator (proven by the number-line pilot `dcfaac7`, calendar
`423c58f`): exported `<name>GradeBandFromGrade(grade?)` mapper (null without a
canonical grade) + `canonical ?? legacyResolver` threading + focused wiring
test + runtime probes at two+ grades. Explicit `config.gradeBand` pins keep
highest precedence everywhere they existed.

## Per-generator results (probe = real `/api/lumina/eval-test`, dev :3000)

| Generator | Defect shape (verified against literal production prose) | Probes |
|---|---|---|
| hundreds-chart (14i) | hard `?? '2'` — never read ctx.grade | G1→1, G3→[2,3,4,5], G4→[3,4,6,7,8] (first band-4), no-grade→2 |
| sorting-station | `/[kK]/` — middle prose "thinking" inverts to K | K→K, G1→1, no-grade K-prose→K |
| number-tracer | `includes('kinder')` — bare-'K' callers inverted to 1 | K→digits ≤9, G1 under K-prose→digits to 20, no-grade→K |
| fraction-circles | tests '1st'/'2nd' — "grades 1-5" matches none → **G1/G2 drew denominators up to 12** | G1→K-2 dens ≤4, G4→3-5 dens to 12, no-grade→3-5 |
| shape-composer | bare-'k' — middle prose inverts to K | K→K, G1→1 |
| net-folder | `includes('5')` matches "grades 1-5" → **'3-4' was unreachable; every elementary lesson ran 4-5** | G3→3-4, G5→4-5 |
| timeline-builder | '1' matches "grades 1-5", 'k' matches "thinking" → **everything landed K-1; 2-3/4-5/6-8 unreachable** | G2→2-3, G4→4-5, G7→6-8, no-grade→K-1 |
| coin-counter (14c) | digit tests match nothing in elementary prose → G2/G3 ran as 1 (contract gap G2) | G2 mixed→band 2 **with a half-dollar drawn** (pool live for the first time), K like→K, G1 like→1, no-grade→1; R9 forks byte-exact |
| states-of-matter | K-2 markers match nothing in "grades 1-5" → **published G1/G2 got 3-5** | G1→K-2 |
| reaction-lab | same | G2→K-2 |
| safety-lab | **kindergarten prose "(ages 5-6)" contains '6' → K landed 6-8** | K→K-2 |
| mixing-and-dissolving | same "(ages 5-6)" trap → K landed 6-7 | K→3-5 (floor clamp) |
| atom-builder, molecule-constructor | "(ages 5-6)" trap (K→6-8); high prose → floor | mapper-tested |
| ph-explorer, equation-balancer, energy-of-reactions | floor-safe today (coincidence, now intentional) | mapper-tested |
| gas-laws, stoichiometry-lab | high prose '12'/"high school" → **every HS lesson incl. grade 9 landed 11-12** | mapper-tested |
| matter-explorer (**census under-count**) | inline bare-'k' resolver — missed because the census grepped for named resolvers ([[value-origin-not-code-touch]] again) | mapper-tested |

LLM-stamped `gradeBand` schema fields (fraction-circles + the five K-band
chemistry generators incl. matter-explorer): with a canonical grade, code now
stamps the band; the LLM echo survives only on the legacy no-grade path.

## Chemistry bite verdict (the queue's "verify before spending" gate)

The queue guessed the chemistry defect "may not bite" (grades 8-12, coarse
bands). **Wrong in an unexpected direction: it bites in the PUBLISHED K-5
band.** safety-lab sent kindergarteners 6-8 content off the '6' in
"(ages 5-6)"; states-of-matter/reaction-lab sent published G1/G2 to 3-5. The
8-12 concern is real too (grade 9 → 11-12 on gas-laws/stoichiometry) but
dormant until that curriculum ships. Bite verified → template applied to all
12 (11 census + matter-explorer).

## Gates

- New suites: hundreds-chart 7/7, sorting-station 5/5, number-tracer 6/6,
  fraction-circles 5/5, sweep-trio (shape-composer/net-folder/timeline) 6/6,
  coin-counter 6/6, chemistry mappers 9/9 — **43 new tests**; non-vacuity
  reverts flipped 3/2/2/2/3/3 of them respectively per generator.
- `typecheck:lumina` **0** · full tsc **803 = baseline** · full vitest
  **1400/1400** (124 files; count includes the concurrent DI session's new
  suites — all green together).
- 21 real-Gemini probes across 13 generators (table above), including every
  previously-unreachable band rung that carries authored demand.

## Follow-ups (queued, not this slice)

- **14i 120-capability half** stays open (extend the chart to a 120-grid —
  fork territory, `qa/reader-fit/hundreds-chart-14i-2026-08-04.md`).
- **14k** (number-line range window) unchanged, mechanism pinned in contract C1.
- coin-counter contract: gap **G2 → close** in `docs/contracts/coin-counter.md`
  on its next `--check`; R10's DEFECT note is now stale.
- The 6 non-probed chemistry wirings (atom/molecule/ph/equation/energy/gas/
  stoichiometry) are mapper-tested + typecheck-gated; their uniform threading
  line matches the probed six. First 6-8/9-12 routing exercise confirms live.
