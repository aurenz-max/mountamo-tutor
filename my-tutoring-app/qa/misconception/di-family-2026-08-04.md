# Misconception Test: Direct Instruction family — 2026-08-04

Gate: **PASS**

This closes DI BACKLOG item 2. The missing `$add-misconception-loop` executor
was restored under `.agents/skills/`, validated, and then used to implement the
reviewed code-owned remediation levers. No diagnosis is sent to the wrapper
Gemini call and no spoken cue, judging contract, correction, move-on, or
completion string changed.

## Verdicts

| Probe | Case | Verdict | Evidence |
|---|---|---|---|
| D | DI golden set | **GENERATIVE / ABSTAINED** | Prior verified run remains authoritative: 10/10 expected, 0 LEAK, 0 OVERREACH (`di-math-facts-2026-07-25.md`). Distiller code was untouched. |
| G | 4 null runs | **clean** | All four packs preserved normal mode/count and serialized no remediation field. |
| G | math: `subtracts_by_adding` | **TARGETED** | Probe D's actual handoff sentence produced two `b=1` counterexamples first; 5/5 subtraction items, answers recomputed, no leak. |
| G | math: `echoes_operand` | **TARGETED** | First two answers differed from both displayed operands and varied both operands. |
| G | math: `reverses_count_direction` | **TARGETED** | First two starts landed immediately below/at legal five boundaries. |
| G | letter sounds: `name_for_sound` | **TARGETED** | `m,s,f,r` led the legal menu draw. |
| G | letter sounds: `confusable_sound_pair` | **TARGETED** | Diagnosed `m/n` shipped together; easy-tier incompatibility saturates without breaking composition. |
| G | word reading: `stops_before_whole_word` | **TARGETED** | Smooth continuant-framed CVC transfer began `sun, run`. |
| G | word reading: `near_neighbor_read` | **TARGETED** | Tight same-position contrast began `cat, hat`. |
| G | word reading: `sounds_out_sight_word` | **TARGETED** | Irregular sight slots began `the, to`; every item remained sight-only. |
| G | sentence reading: `drops_function_word` | **TARGETED** | Two sentences contained the diagnosed medial `is`. |
| G | sentence reading: `near_neighbor_word` | **TARGETED** | Two legal sentences contained the reviewed `hen/pen` contrast. |
| G | sentence reading: `word_order_or_addition` | **TARGETED** | Repeated grammatical-slot sentences shipped while all items remained hard-tier 7–8 words. |
| R | journey + scope matrix | **CLOSED** | Backend pytest 9/9. |
| R | S4 Firestore exposure | **pass** | Disposable probe returned primitive-scoped key `di-math-facts` and cleaned up. |

Probe G result: **11/11 TARGETED**, 0 DEAD-FIELD, 0 DRIFTED, 0 LEAKY.
Every runtime response preserved its requested eval mode and item count. The
hard sentence probe preserved `supportTier=hard` and the 7–8-word band. No
serialized response contained `remediationFocus`, a diagnosis sentence, or a
remediation enum.

## Implementation contract

- Each pack exports a typed, pure `resolveDiRemediationMove` resolver.
- Blank, vague, unsupported, and cross-mode diagnoses are no-ops.
- Named/review anchors and L4 structural windows outrank remediation.
- Normal dosage is up to two targeted items followed by legal transfer items;
  capacity limits log honest saturation.
- Logging is limited to `[DiRemediation]` safe enums, requested/actual counts,
  and skip reasons.

## Verification

- Focused DI suites: **91/91**.
- New remediation contracts: **20/20**.
- Revert non-vacuity: math pilot **4 failures** with the resolver removed;
  family sweep **6 failures** with consumption disabled.
- Full frontend Vitest: **1,569/1,569**.
- Backend round trip: **9/9**.
- S4 Firestore exposure: **pass**.
- `typecheck:lumina`: the four DI generators/tests are clean; the repository
  gate remains red only on two unrelated in-flight `gemini-story-map.ts`
  contract errors (`supportTier`, `distractorCharacters`).

**Not verified here:** a new S1 browser capture. The family already has a real
live capture from the 2026-07-26 DI sitting; this slice changed only S5 content
selection and no browser/audio behavior.

**Distiller handoff:** the subtraction pilot used the prior Probe D output:
“When subtracting one, the student says the number that comes after the given
number instead of before it.”
