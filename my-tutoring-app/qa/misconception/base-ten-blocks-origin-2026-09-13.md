# Base-ten blocks originates an observation — 2026-09-13

Gate: **PASS for capture, storage, delivery and generation with synthetic evidence.** Base-ten-blocks `read_blocks` was a consumer only: its corrections never reached the distiller. It now produces a skill-scoped observation, and that observation drives the next Grade 4 `read_blocks` activity through the shared planner. Not claimed: microphone capture, teaching effectiveness, learner transfer, or resolution.

Executor `/add-misconception-loop`, row `base-ten-blocks / read_blocks` of `artifacts/math-pedagogy-review/plans.json`. Built alongside four sibling sessions (bar-model, number-line, fraction-bar, fraction-circles). Delivery is shared, and the ownership split is below.

## What changed

| Station | Before | Now |
|---|---|---|
| S1 evidence | `diagnosisObservation` expected `'the place value of those blocks'` / `'a number'`; evidence sent only when accuracy < 60 | `read_blocks` corrections carry the real ask, the mat value, the block size and the answer word (`readWorthWord` / count word), which is the same task text as the response ledger. The runner's phases and tutor correction line reach `student_work.diagnosisEvidence` whatever the average, with `firstResponseScore` (share of steps right on the first try). `regroup` supplies none. |
| Catalog | no `misconceptionScope` | `misconceptionScope: 'skill'`. Only `read_blocks` supplies evidence, so the other modes skip capture without a model call. |
| S3 store | chart-only canonical scope | `SERVER_DELIVERED_SOURCES` (backend `misconception_opportunities.py`, owned here). For members, capture must be skill-scoped at a resolvable published MATHEMATICS scope, else `stored: false`. `scope_context` is stamped on the ordinary write path too, with no receipt revision. `/generation-context` blanks their prose, so no client `remediationFocus` and no client remediation tag. `submission_service` and `resolve_misconception` never score/tag-resolve them. Without the last three, opting in would have silently armed the legacy loop: prose in the client manifest, and resolution at score ≥ 80. |
| S4 delivery | `generateWithBaseTenObservation`: chart record only, pinned to `NBT004-01-b` | Blocks routes through the shared `generateWithLearningObservations` (`/learning-observation-context`, owner claude-web-tutor-d8). It carries observations from any member source whose stamped subject, grade and skill match. Eligibility: Grade 4, `read_blocks`, medium. **Scope change:** no longer pinned to one subskill. The chart-only wrapper and the `sourcePrimitive` field are retired; opaque IDs and `source: 'saved-observation'` only. |
| S5 execution | move dropped silently when the model labelled the band `2-3` | `gradeBand` follows the code-owned range, which is the prompt's own mapping. A validated move is always reported: numbers outside 1000–9999, or decimal mode, give `insufficient-capacity`. |

Production code is about 60 changed lines plus 25 removed. Tests (+4 backend, +3 frontend mounted/registry cases, +1 generator case) and the two probes are several times that.

## Verification

- **Unit:** mounted `BaseTenBlocksDi` with the real runner. A capped worth item (bare count three times) gives factual phases and a Tier-A correction line; capture posts `base-ten-blocks`/`skill`/`NBT004-01-b` with the phase packet. A passing run with one missed step (67%, first-response 83%) keeps the evidence but capture skips it. `regroup` never calls the model. Registry path: `generateComponentContent` → shared wrapper → real blocks generator. A delivered observation changes compiled mats from 0 to 2 contrasts, abstention keeps the baseline numbers, ineligible or anonymous requests never fetch, and a client-supplied list is dropped. Mutation check: reverting the evidence gate or the answer word fails two tests. Backend: primitive scope and unresolved scope refused, `scope_context` written, prose blanked, a score-95 tagged submission leaves the record active.
- **First-response gate (added after the shared gate landed):** a step corrected once still scores 67, so the core signature (bare count on every worth step, right after one correction) passed the average and never diagnosed. Blocks now reports `firstResponseScore`; claude-web-tutor-03 changed the shared distiller line so it no longer claims every item ends solved. Mounted test: that run submits 67% and first-response 50%, and capture calls the distiller. Real distiller (`first-response-report.json`): corrected-once bare count diagnosed 2/2, corrected-once inconsistent values abstained 2/2, no answer words. Removing the field fails 3 tests.
- **Suites:** full frontend **6,077 passed / 10 skipped** (`artifacts/blocks-origin-observation/full-frontend-tests.log`, taken while sibling sessions were editing). Backend misconception suites **64 passed** (60 baseline + 4). Full `tsc` shows no errors in the files touched here. `typecheck:lumina` 0 after the first-response change.
- **Real distiller, Probe D** (`artifacts/blocks-origin-observation/report.json`). Evidence built from production item text in the runner's shape. Bare count for worth across two block sizes: diagnosis 2/2, e.g. *"interprets a prompt asking what a group of base-ten blocks is worth altogether as asking for the count of those blocks"*. Inconsistent wrong values: abstain 2/2. No transcript: abstain 1/1. No answer words in any output.
- **Real planner and generator, Probe G:** expectations were declared in the script before running.
  - **Run 1** (`run-1-band-drift/`): 9/11, plus follow-ups. Every failed draw had `gradeBand: '2-3'` over four-digit numbers. The old `gradeBand === '4-5'` gate dropped the validated move with no metadata. This is execution drift, not planner abstention; 6 of 15 Grade 4 draws carried the wrong label.
  - **Run 2**, after the fix: **12/12**. The distilled text, three paraphrases (including the ambiguous one that failed in run 1) and the mirror error (value said for count, 3/3) all target. Unrelated, uncertain, and nearby-but-untaught regrouping (3/3) abstain. All bands `4-5`.
  - Of 8 positive run-2 draws, 3 were `targeted` and 5 `already-targeted`: ordinary mats often contain the contrast already.
- **Cross-representation, planner only:** the blocks-worded hypothesis offered to the chart capability abstained in both runs. Production chart delivery does not receive blocks-origin observations.
- **Authenticated** `pvc-http-qa-cee522b2b2fd48148022f628a1452f91`: **PASS, cleanup verified absent**. Firebase user and disposable learner; real distiller on the Probe-D packet; capture refusals (primitive scope, unpublished subskill); canonical scope stamped with no receipt identity; owner profile shows the observation with response `four`; direct client read/write 403; unsigned delivery call 403. Two authenticated blocks draws are `targeted`. Compiled mats: 2 thousand-blocks (two thousand) vs 2 hundred-flats (two hundred) in 2607/8204; 8 vs 8 in 8707/5803. The chart draw was unadapted with no receipt. The record stayed active and unresolved, and no opportunity documents were written. No canonical submission. Run after the 08:31 signing-key fallback; earlier signed calls from this morning returned 403.

## Residual (queued in `qa/di/BACKLOG.md` item 18)

1. **One missed step in a passing run still saves nothing:** three mats with one capped worth step give 67% and first-response 83%, above the gate. That is a single-instance miss, not a pattern, so this is left as designed; `success` stays `accuracy ≥ 60`.
2. **Reverse bridge:** chart generation does not receive blocks-origin observations. Exposure direction needs a user ruling.
3. `regroup` has no evidence producer and no capability.
4. `already-targeted` is frequent (5/8), and the authenticated baseline also held a contrast; this is the ordinary mat-variety limit in item 18.
5. Blocks-origin hypotheses have no resolution path by design; re-detection overwrites the slot.
6. Microphone path (spoken wrong worth → stored observation → adapted next activity) → HUMAN-CHECKS.

Reproduce: from `my-tutoring-app`, `node scripts/probe-blocks-origin-observation.mjs` (Next on :3000). From `backend`, `py311env python -W ignore scripts/probe_place_value_authenticated.py --blocks-origin`. It reads Probe D's saved packet and prints nothing on success; read the newest `authenticated-report.json`.
