# Session 03 — ten-frame runtime adoption

**2026-09-17 update:** The user explicitly authorized this bounded adoption. It is
implemented in the existing lesson host with the actual voice judge and frame.
Make-ten Grade 1 is the live-model pilot. Real component tests also cover preserved
mid-build counters, late voice verdicts, StrictMode, stop and planned closing.
Subitize help remains unavailable; other modes need their own live review.
See [pilot evidence and remaining browser check](../tutor-reports/ten-frame-runtime-live-2026-09-17.md).

Requires session 02's verified shared lifecycle. Scope is ten-frame plus its adapter
and tests. Read the runtime README, `TenFrame.tsx`, `tenFrameScript.ts`, live-start
tests, primitive contract and the shared runner's new opt-in API. TenFrame already
had uncommitted changes before the infrastructure slice; preserve them.

Register truthful semantic state from the real DI item/phase, frame, attempts and
ownership. Advertise only runner-backed legal actions. Do not add a separate phase
counter or special transport branch. Configure host-owned planned completion so
`[TF_COMPLETE]` cannot end the whole conversation or duplicate plan completion.
Standalone completion must retain its established behavior.

Prove suspension at each response style being enabled (gesture/build and spoken
judgment). Preserve counters and phase through the structured help shell. Do not
rebuild the frame from generated JSON on return. Until a phase can suspend safely,
do not advertise its support detour. Examples must match the actual operation and
grade; record current-answer exposure and use a new item for independent evidence.

Exit: real TenFrame plus real runner tests for mid-build help, pending judgment,
late verdict/stimulus after return, opener ownership, one completion after closing
audio, unsupported actions, and fresh transfer. Verify generated pilot content
through the existing evaluation tooling only if content contracts changed.
Run shared runtime, runner and ten-frame suites and Lumina typecheck. Do not expand
to another DI primitive in this session.
