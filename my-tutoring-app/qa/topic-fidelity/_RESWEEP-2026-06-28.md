# Topic-Fidelity Re-Sweep of the "code-picked / low payoff" bucket — 2026-06-28

Re-triage of the 31 generators the 2026-06-27 MATH_TRIAGE flat-bucketed as "DEAD-FIELD,
code-picked/na scope — theme-flavor only, low payoff". That bucket was mis-axed (it bucketed
by "does code touch the value?"). Re-classified by the correct axis — **"is code-picking
required for CORRECTNESS?"**. Every CLASS-1 verdict below is **probe-verified live** (intent
varied, topic fixed), not grep.

## Method note — why the original flat verdict was unsafe
A static read / fast pass mis-called several generators:
- **comparison-builder** was called "LLM authors only title/description" — false; it authors every number.
- **shape-builder** was called a dead field — false; it already honors intent via `buildScopePromptSection`.
- **net-folder** was called CLASS-1 — only half true; the solid is code-picked (`randomSolid`).
Only reading the value-origin + a live probe settles it. (Confirms the user's "verify, don't grep" call.)

## CLASS-1 — value LLM-authored → TIER-1 wire, probe-verified

| Generator | Wire | Probe (intent varied / topic fixed) | Verdict | tsc |
|-----------|------|--------------------------------------|---------|-----|
| comparison-builder | `ctx.intent` + focus block | teen→11-20, small→1-5, generic→full band | **HONORED** | 1417 |
| time-sequencer | `ctx.intent` folded into tierSection (5 sub-prompts) | half-hour→:00/:30, o'clock→:00, morning→AM, evening→PM | **HONORED** | 1417 |
| 3d-shape-explorer | `buildScopePromptSection(ctx.scope)` | "cylinders/cones"→cone+cylinder, "cubes/prisms"→cube+prism | **HONORED** | 1417 |
| shape-sorter | `buildScopePromptSection(ctx.scope)` | "triangles+hexagons"→{tri,hex} every challenge; "circles+squares"→circle/square-led | **HONORED** | 1417 |
| shape-tracer | scope threaded into `generateSetup` (where targetShape is chosen) | "triangles"→all triangles; "hex+pent"→pentagon+hexagon surface | **HONORED** | 1417 |
| shape-builder | *(already wired — no change)* | "triangles"→all sides:3; "hexagons"→all sides:6 | **ALREADY HONORED** | 1417 |

## CLASS-1 but NA / hybrid — wired or deferred with reason

| Generator | Finding | Action |
|-----------|---------|--------|
| spatial-scene | Intent for this primitive is the position-words objective, NOT a scene theme. Scene theme is intentional random flavor (`randomTheme`). No crisp intent lever (NA-leaning, WRONG-PRIMITIVE per skill). | Wired `scopeSection` anyway (standard contract — binds any range the objective implies; harmless). No crisp probe delta — by design. |
| net-folder | Hybrid: the main scopable lever (which solid) is **code-picked** (`randomSolid(gradeBand)`, l.915) → CLASS-3/Tier-2. Only surface_area face-dims are LLM-authored. | NOT Tier-1. Deferred to Tier-2 (intent→solid resolver feeding `randomSolid`). |

## CLASS-2 — code-derived for CORRECTNESS → genuinely theme-only (10) — LEAVE AS-IS
place-value, ordinal-line, sorting-station, systems-equations, strategy-picker,
regrouping-workbench, percent-bar, length-lab, balance-scale, slope-triangle.

## CLASS-3 — entropy code-pick → TIER-2 resolver (IN PROGRESS)

Each owns its band in code for variety, not correctness. The fix is a **shared Tier-2 resolver**
— `scopeRangeResolver.ts` `resolveScopeRange(scope, gradeLevel, quantity, ceiling)` — one
flash-lite call (temp 0) generalizing number-line's `resolveTopicNumberRange`. It turns
topic+intent into the `{min,max}` the code-owned band-picker needs, **hard-clamped to the
ceiling** (only narrows, never widens) and **null on no-intent/failure** (→ grade default, no
regression). Gated inside the resolver on scope carrying intent/objective.

**Two sub-kinds within the 14:**
- **Numeric-range scopable (9)** — a `{min,max}` cap fits: array-grid ✅, circle-explorer,
  area-model, polygon-area-builder, angle-workshop, matrix, measurement-tools, factor-tree,
  fraction-bar (denominator FAMILY — may need a set-picker, not a range; under review).
- **Categorical/geometric (5)** — "scope" is which pool entry (a rule, scenario, shape), NOT a
  `{min,max}`: function-machine, transformation-lab, shape-composer, histogram, two-way-table.
  The range resolver does not fit; these need a category resolver or are NA. Triage separately.

| Generator | Wire | Probe (intent varied / topic fixed) | Verdict | tsc |
|-----------|------|--------------------------------------|---------|-----|
| array-grid | `resolveScopeRange` → clamp dimension band | "up to 5"→maxDim 5; "through 8"→maxDim 7; generic→full band | **HONORED** | 1417 |
| factor-tree | resolver → filter composite pool (correctness-guarded) | "within 12"→{6,10,8,12}≤12; "up to 24"/generic→max 24 | **HONORED** | 1417 |
| measurement-tools | resolver → filter width pool | "1–3in"→max 3; "up to 8in"→max 6; generic→max 8 | **HONORED** | 1417 |
| circle-explorer | resolver → module radius cap, clamp at pick-time in 5 builders | "radius up to 5"→max 5; "up to 12"→max 12 | **HONORED** | 1417 |
| matrix-display | resolver → clamp `rangeForGrade` ENTRY range (not dims; inverse untouched) | "1–4"→max 4; "up to 9"→max 9; generic→8 | **HONORED** | 1417 |
| polygon-area-builder | resolver → module dim cap, `capDims` floor-respecting clamp in 6 builders | "up to 6"→maxDim 6; "up to 16"→max 12; generic→13 | **HONORED** | 1417 |

**Bugs the live probe caught + fixed (would have shipped silently on a grep pass):**
- measurement-tools: the pool-too-small fallback fabricated widths OUTSIDE the cap
  ("1–3in" leaked a 6). Fixed: skip the offset-fabrication when a cap is active.
- factor-tree: the odd-inclusion VARIANCE guard blocked scope narrowing when the in-range
  composites were all even ("within 12" → no odd composite → kept full pool, max 21). Fixed:
  scope-honoring (pedagogy #1) wins over the odd-variance nicety; downstream guarantee
  degrades gracefully to all-even. (Composite-preservation is inherent — pools are composites.)

### Remaining numeric — RESOLVED
- **circle-explorer** ✅ wired + probe-verified (module radius cap; clamp at pick-time so all derived values stay consistent).
- **matrix-display** ✅ wired + probe-verified (entry range only; inverse left untouched for its det=±1 constraint).
- **polygon-area-builder** ✅ wired + probe-verified (`capDims` respects each figure's structural floor; trapezoid bottom-base bounded).
- **area-model** ⏸ DEFERRED with reason — its factor magnitudes are STRUCTURALLY mode-owned: the
  whole point is decomposing a 2-/3-digit number into place-value grid parts (`build_model` needs a
  2-digit factor, `multiply` a 3-digit). A range clamp below the mode's digit count makes
  `decomposeByPlace` yield too few parts → empty pool / broken grid. This band is CLASS-2-like
  (mode owns it for structural reasons), not a free CLASS-3 range. Not safely scopable by {min,max}.

### Need a DIFFERENT resolver (range tool does NOT fit — do not force it)
- **fraction-bar** — lever is a DENOMINATOR FAMILY {2,3,4,6,8}, not a range. Needs a
  `resolveDenominatorSet` (set-picker) resolver. Deferred — designing separately.
- **angle-workshop** — HIGH RISK: clamping degrees deletes the obtuse/supplementary angles
  specific eval modes REQUIRE (capping at 90° trivializes `measure` hard tier, kills
  `classify_pairs` supplementary). Needs a mode-aware resolver. Deferred.

### Categorical (5) — "scope" is which pool entry, not a {min,max}
function-machine (which rule), histogram / two-way-table (which scenario), shape-composer /
transformation-lab (which shape). Range resolver N/A; need a category/theme resolver or NA. Triage separately.

## Tally
- **6 CLASS-1 now HONORED** (5 wired + probe-verified, 1 already-honored).
- **2 reclassified** out of Tier-1 with reason (spatial-scene NA, net-folder Tier-2).
- **10 CLASS-2** confirmed theme-only (correctness-locked) — correct to leave.
- **14 CLASS-3** Tier-2 resolver candidates — the real remaining scope work.
- tsc 1417 throughout (baseline 1419 global; no new errors).

The original "21 of 31 mislabeled" headline holds: 6 were real Tier-1 fixes (now done),
14 are Tier-2 candidates (not dead ends), 1 hybrid. Only the 10 CLASS-2 were correctly
"theme-only".
</content>
