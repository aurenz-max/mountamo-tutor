# You & Me — L2 tutoring scaffold

2026-09-07. Catalog scaffold added for both L1 modes. The shared spoken runner remains the connection, speech and progression owner; no competing useLuminaAI connection or duplicate per-turn trigger was added.

| Check | Result |
|---|---|
| Tier 1 connection audit | PASS, zero findings |
| Tier 2 real prompt assembly: describe_action | PASS, every template/context value resolves |
| Tier 2 real prompt assembly: describe_independent_action | PASS, correct mode and named roles |
| Tier 2 mixed session | PASS; transition context additionally covered by tests |
| eval-test, both pinned modes | PASS, six turns each |
| Focused tests | 41 pass; 8 existing shared-audit regression tests also pass |
| Full TypeScript comparison | Zero new diagnostic lines against baseline |
| Lumina typecheck | PASS, zero errors |
| Tier 3 real microphone behavior | Not driven; remains owed |

Nine runtime keys: challengeType, scene, speaker, listener, actor, taskFocus, modeHint, currentTurn, totalTurns. The production pack supplies the same context at connection and after each turn. Mode-specific hints never supply the target pronoun or model sentence. Struggles cover fixed pronoun use after role switches, named-scene echoes, bare pronouns, mismatched self referents and omitted self forms.

Four existing cue tags are documented: YOU_AND_ME_ITEM (opening/progression), YOU_AND_ME_HEAR (scene replay), YOU_AND_ME_MOVE_ON (correction cap), YOU_AND_ME_COMPLETE (closing). Correct/incorrect replies remain governed by the existing item verdict protocol. Directives keep context updates silent, prevent extra greetings/questions, distinguish help requests from answers, and reserve model sentences for the existing post-error correction only.

The generic QA scanner previously recognized only direct useLuminaAI bags. It now verifies inline contextFor packs consumed through useJudgedScriptRunner, including shared silent transports. Negative tests detect a missing consumer, absent context key and non-silent transport. The Tier 2 You & Me probe uses the production item builder and pack context, so actor/speaker indices cannot masquerade as the names actually sent to the tutor.

Raw evidence: `qa/eval-reports/you-and-me-l2-describe_action.json`, `you-and-me-l2-describe_independent_action.json`, `you-and-me-l2-mixed.json`, and `you-and-me-l2-eval-*.json`.

Live acceptance: ask for help before answering; verify no target/model is given. Try a wrong perspective and a mismatched self form, then a valid paraphrase. Confirm correction/retry, silent internal messages, mode switches and final submission. Static and assembled-prompt checks do not establish live speech compliance.

Follow-ups completed: [support tiers](../eval-reports/you-and-me-l3-2026-09-07.md) and [structural assessment](../eval-reports/you-and-me-structural-assessment-2026-09-07.md). The current contract remains support-only. Next: outstanding browser/microphone acceptance.
