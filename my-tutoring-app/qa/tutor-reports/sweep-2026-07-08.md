# Tutor-Test Sweep — 2026-07-08 (first run)

First full Tier-1 audit of every catalog tutoring scaffold via `/api/lumina/tutor-test`.
Raw JSON: `sweep-2026-07-08.json`. Harness: `service/qa/tutoring/scaffoldAudit.ts`.

## Rollup

| Metric | Count |
|--------|-------|
| Scaffolds audited | 130 |
| PASS | 72 |
| WARN | 12 |
| **FAIL** | **46** |
| Hook wired, no scaffold (L0/L1 — the `/add-tutoring-scaffold` queue, not bugs) | 40 |
| Stale primitiveType ids | 0 |

Finding totals: **159 HIGH key-resolution breaks** (84 `{{var}}` + 75 contextKey), 3 HIGH answer leaks, 1 HIGH orphan, 40 WARN dynamic-bag downgrades, 5 WARN silent-tutor, 4 WARN dead directive tags, 3 WARN unparsable bags.

## Pattern 1 — Scaffold↔component key desync (SP-27, the dominant failure)

44 of 46 fails: the catalog scaffold references `{{vars}}`/contextKeys that the component's
`aiPrimitiveData` never emits. Backend renders each as the literal `(not set)` — silently.
Scaffolds were authored against an intended data model; the component bag was written
(or later refactored) independently, and nothing ever checked the join.

Worst offenders (all keys broken — tutor is fully context-blind):

| Primitive | Broken keys | Bag actually contains |
|-----------|------------|----------------------|
| sound-wave-explorer | 12 (vibrationObject, forceLevel, medium, phase…) | theme, gradeLevel, challengeCount |
| push-pull-arena | 12 | (similarly minimal) |
| length-lab | 9 | — |
| gravity-drop-tower | 9 | — |
| sentence-analyzer | 9 | — |
| time-sequencer | 8 | — |
| function-machine | 8 | — |

Full fail list: fast-fact, how-it-works, deep-dive, bar-model, fraction-circles, area-model,
tape-diagram, factor-tree, ratio-table, balance-scale, function-machine, coordinate-graph,
counting-board, comparison-builder, shape-builder, addition-subtraction-scene,
3d-shape-explorer, shape-tracer, strategy-picker, length-lab, analog-clock, coin-counter,
time-sequencer, spatial-scene, shape-composer, net-folder, parameter-explorer,
equation-workspace, function-sketch, distribution-explorer, construction-sequence-planner,
hydraulics-lab, transport-challenge, energy-of-reactions, mixing-and-dissolving,
planetary-explorer, sound-wave-explorer, push-pull-arena, race-track-lab, gravity-drop-tower,
sentence-analyzer, sentence-builder, word-sorter, calendar-explorer, timeline-builder
(+ machine-profile, orphan).

**Fix pattern:** per primitive, add the missing derived keys to `aiPrimitiveData` (preferred —
the scaffold copy is the pedagogically-reviewed artifact) or rename the scaffold reference to
a key that exists. Verified template: dot-plot (fixed this run — `dataCount`, `parallel` added
to the bag; now PASS).

## Pattern 2 — Answer leak in spoken scaffold text (TU-1..TU-3)

`{{correctAnswer}}`-class keys interpolated into scaffoldingLevels / struggle responses —
scripts the tutor reads aloud. (In contextKeys/RUNTIME STATE it's fine — tutor-reference only.)

- **fast-fact** — `{{correctAnswer}}` in a scaffolding level
- **comparison-builder** — `{{correctAnswer}}` in a scaffolding level
- **word-sorter** — `{{correctCategory}}` in a scaffolding level (also unresolvable — double bug)

## Pattern 3 — Orphan scaffold (TU-4)

- **machine-profile** — full tutoring block in `catalog/engineering.ts`, component never calls
  `useLuminaAI`. Scaffold is dead weight; students get the generic tutor.

## Pattern 4 — Silent tutors (WARN)

No `sendText` anywhere: vehicle-comparison-lab, propulsion-timeline, constellation-builder,
story-map, evidence-finder. Tutor greets once, then never speaks.

## Pattern 5 — Dead directive tags (WARN)

aiDirectives promise a trigger no code ever sends: how-it-works `[STEP_NAVIGATION]`,
flight-forces-explorer `[AOA_CHANGED]`, syllable-clapper `[PRONOUNCE_WORD]`,
picture-vocabulary `[SPOKEN_NO_SPEECH]`.

## Verified this run

- **dot-plot** — `{{dataCount}}` + `parallel` contextKey desync FIXED (bag keys added), re-audited: PASS.
- Analyzer spot-checked against source for sound-wave-explorer (real) and machine-profile (real orphan).

## Next actions

1. Fix the 3 answer leaks (CATALOG copy) — pedagogy rule #1, cheapest wins.
2. machine-profile: wire `useLuminaAI` or delete the scaffold.
3. Sweep Pattern-1 fixes family-by-family (physics sims first — fully context-blind).
4. WARN dynamic-bag primitives: verify via `&probe=1` before trusting.
