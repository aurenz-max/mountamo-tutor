# Topic Trace: "Interpret simple neighborhood map symbols using a key or legend" (Grade 1) — 2026-08-01

Published subskill: `SS003-02-a` (target primitive: `foundation-explorer`).
Scope intended by the subskill: interpret simple, high-contrast neighborhood symbols through a supplied map key.
Part of the 2026-08-01 EMERGING demand census.

## Components

| Component | In scope? | Generated evidence / issue | Broken link | Fix target |
|---|---:|---|---|---|
| foundation-explorer | yes | map/symbol/key definitions and symbol self-checks | — | — |
| sorting-station (`sort_one`) | yes | Grade-1 real-object vs symbol sort | — | — |
| vocabulary-explorer (`explore`) | partial | requested four terms but adds a fifth (`Compass`) | GENERATOR (mild) | count/scope binding |
| deep-dive (`recall`) | yes | symbol/key explanation; payload retains generic grades-1–5 prose | — | grade-contract item |
| blueprint-canvas | yes (scope) | map-symbol placement, but generic drafting data keeps `showMeasurements: true` | COMPONENT / routing | EMERGING interaction audit |
| fast-fact | **wrong band** | correct map-symbol topic but payload is `gradeBand: 3-5`, with streak/accuracy chrome and 10 challenges | GENERATOR / grade context | EMERGING grade-contract item |
| knowledge-check (`mixed`) | partial | routes in every census lesson; symbol matching is text-description-primary rather than interpreting rendered symbols | GENERATOR / modality | EMERGING knowledge-check audit |

## Scope drops

None severe. `Compass` is topical but exceeds the four-term intent; the main failures are Grade-1 presentation and modality, not subject scope.

## Reader-fit signal

`fast-fact` is the clearest wrong-band payload in the SS sample (`3-5` on a Grade-1 lesson). The final knowledge check assesses map symbols largely by reading descriptions such as "a green tree icon," which turns visual-symbol interpretation into text comprehension.
