# Topic Trace: "Counting to 10" (Elementary, Grades 1-5) — 2026-05-31

Scope intended by the topic: **values ≤ 10** (count, recognize, order, and build sets of 1–10).

Manifest produced 9 components across 3 objectives. Objectives and intents all kept the 1–10 scope cleanly — no OBJECTIVE or INTENT link breaks. One generator overshoots in its output.

## Components

| Component | In scope? | Largest student-facing value | Broken link | Fix target |
|-----------|-----------|------------------------------|-------------|------------|
| number-sequencer | ✓ | 10 (`startNumber:10` counts **backward** → 9,8,7) | — | — |
| number-tracer | ✓ | digit 8 | — | — |
| fast-fact | ✓ | 10 | — | — |
| counting-board | ✗ | **12** butterflies on screen (c7 compare) | GENERATOR | scopeContext rollout |
| ten-frame (apply) | ✓ | 9 | — | — |
| comparison-builder | ✓ | 8 | — | — |
| ten-frame (create) | ✓ | 9 | — | — |
| sorting-station | ✓ | maxCategories 2; bin targets ≤ 10 | — | — |
| knowledge-check | ✓ | 10 | — | — |

## Scope drops

### counting-board — generates a 12-object compare challenge for a "to 10" topic
- **Chain:** objective "Apply counting to a small set of objects" (✓ scope ≤10) → intent "find the total quantity **up to 10**" (✓ restates ceiling) → data challenge `c7` `{type:"compare", count:12, targetAnswer:8}`.
- The student sees **12 butterflies** rendered on screen ("Two groups of butterflies are landing. Which group has more?"). The compared answer (8) is in scope, but the total displayed quantity (12) exceeds the 1–10 ceiling the topic and intent both set.
- **Broken link:** GENERATOR. The objective and intent both carried the ≤10 ceiling, but the generated `data` blew past it.
- **Fix target:** `gemini-counting-board.ts`. Confirmed it does **not** call `buildScopePromptSection` from `scopeContext.ts` (only `gemini-number-sequencer.ts` and `gemini-ten-frame.ts` do). This is a direct scopeContext rollout target — wiring the scope section would bind the generator's `count` values to the objective ceiling.

## Notes (in scope, no action)
- `number-sequencer` seq5 starts at 10 but counts backward — stays ≤10. Not a drop.
- `number-tracer` describes "writing numbers 0–10" / gradeBand K; digits used 2,3,5,7,8 — all in scope.
- `sorting-station` is the broadest (categorization by attribute), but bin target counts stay ≤10; the activity is on-objective for "create a group that matches a number 1–10."
