# Contract check: knowledge-check — 2026-08-02 (reader-fit 14f)

**Edit under guard:** precise Grade-1 reading-load constraints plus schema-backed visual evidence
for inherently visual tasks. **Verdict: COMPATIBLE** — this builds contract gaps G1/G2 under the
existing C1 band gate and C2 per-problem ruling; it does not fork an observed consumer.

## Other-consumer probes

| Req | Consumer / property | Probe | Result |
|---|---|---|---|
| R1 | answer identities resolve | knowledge-check oracle + full suite; live MCQ IDs inspected | **PASS** |
| R2 | K picture/read-aloud/type floor | focused K mock + real-Gemini K recall; existing MCQ reader-fit tests | **PASS** — 3 emoji choices, MCQ, no G1 panel |
| R3 | grade realization, Bloom thinking | G1 invention `analyze` eval-test | **PASS** — comparison thinking, 13-word stem, 4 short options |
| R4 | single/blend/mixed task identity | analyze + mixed eval-test; map final trace | **PASS** — final retained MCQ/TF/fill-in diversity |
| R5 | visual task uses visible evidence | both census replays | **PASS** — map keys/symbols and invention change rendered |
| R6 | viewport-gated MCQ/TF voice | component/catalog untouched; 230 contract-facing tests + full suite | **PASS (diff + regression evidence)** |
| R7 | `::pN` composite gate | KnowledgeCheck/KindergartenStage untouched; full suite | **PASS (diff + regression evidence)** |
| R8 | final precise grade + attribution | `flattenManifest.test.ts` in 230-test set; live trace echoes `objectiveGrade: Grade 1` and stamps objective ids | **PASS** |
| R9 | evidence generated atomically | non-vacuous missing-visual test rejects with “All generators failed”; both flat shapes live | **PASS** |

## Conflict rulings

- **C1 remains resolved:** only exact Grade 1 gets the new reading/schema rules. K clears
  `visualType` and retains its separate picture-option/read-aloud contract; grades 2+ keep their
  previous broad-band behavior.
- **C2 remains resolved:** visual matching is converted per item. Sibling TF, fill-in,
  categorization, and sequencing items remain available; the map replay demonstrated a mixed final.

## Gaps

- **G1 BUILT:** `ctx.grade` reaches both planning stages; Grade-1 structure is schema-bounded.
- **G2 BUILT:** required flat visual fields reconstruct existing bounded renderers; missing evidence
  is rejected.
- **G3 OPEN:** true/false PRE surface parity is untouched and remains a separate follow-up.
