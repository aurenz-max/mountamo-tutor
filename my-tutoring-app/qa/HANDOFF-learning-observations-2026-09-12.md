# Learning observations: frontend-first continuation

Status: current handoff. Supersedes the implementation-status claims in `HANDOFF-misconception-opportunity-contract-2026-09-12.md`; that document remains historical design context. Work is uncommitted and spans several earlier slices. Preserve the working tree. Do not restart the architecture from that older handoff.

## User direction

The user wants a general shared evaluation → LLM distillation → stored learning observations → activity-specific context loop. Build from visible frontend product features, not speculative backend infrastructure. The existing profile must reveal what was inferred, why, and how it affects the next activity. Reuse shared primitives, runners, evaluation, manifest and generation paths. Avoid asking the user to repeat a multi-minute lesson to test storage; use the automated harness first.

## Demonstrated now

- **2026-09-13 (later): backend is primitive-agnostic.** `SERVER_DELIVERED_SOURCES` and the reviewed-objective allowlist no longer exist; a producer declares `observationDelivery: 'server'` in its catalog entry, capture relays it, and server delivery is a stamped-`scope_context` record property. See the ADR amendment and BACKLOG item 18.
- **2026-09-13: base-ten-blocks read_blocks originates observations.** Blocks corrections now save a skill-scoped observation; `SERVER_DELIVERED_SOURCES` keeps member prose off the client manifest and blocks score/tag resolution; blocks read the shared `/learning-observation-context` delivery (any member source, same published skill). [Report](misconception/base-ten-blocks-origin-2026-09-13.md).
- **Latest correction: shared LLM applicability replaces generation regex.** `planLearningAdaptation` interprets observations against declarative activity tasks and supported moves, selecting a validated enum or abstaining. Place Value passed first, then Blocks: 14 real cases including semantic paraphrases and negative observations. Fresh authenticated one-source/two-primitive run passes with cleanup verified. Legacy regex remains only for older retest receipts. Read [the applicability report](misconception/shared-learning-applicability-2026-09-12.md) before the historical details below. Profile-wide retrieval and strength/support generation remain open.

- **Continuation: one misconception impacts two primitives.** The user authorized a bounded Place Value → Base-Ten Blocks bridge for digit/face-value-for-worth confusion. Both retain Grade 4 `NBT004-01-b`, medium support and their own modes (`compare` / `read_blocks`). Blocks use the existing count/worth script and a small deterministic selector; they now also emit the shared response ledger. Same saved source, no copied diagnosis and no block-side resolution. Real LLM/auth/store/generation harness passes with two draws per primitive and cleanup verified. [Bridge report](misconception/place-value-base-ten-bridge-2026-09-12.md). The review HTML now embeds four actual contrasts.
- **Continuation: shared strengths/support capture and storage implemented.** After accepted normal submission, `EvaluationContext` invokes evidence-gated `captureLearningObservation` independently of aggregate success and misconception capture. Authenticated POST/GET `/learning-observations` stores one immutable tentative observation per client attempt/primitive in a separate collection; the profile returns the latest 50 alongside existing diagnoses. My Progress refreshes after confirmed storage. Validated citations, partial assistance, unverified speech and client-reference limitations remain visible. No misconception transition or learning-progress write is added. The manual tester preview remains explicitly unsaved.
- **Continuation: successful-response preview implemented.** Place Value opts into a shared factual response ledger (affirmed and corrected); canonical `student_work.learningResponses` retains it. The tester exposes recorded responses and an explicit “Preview strengths and support” LLM action, using the shared observations panel with an unsaved label. Requires successful recorded responses on two distinct items in the same phase; missing transcripts and scores alone abstain. Source speech is unverified transcription, correction/replay history is partial, and zero corrections never certifies independence. This preview does not persist inferred strengths/support or drive generation.
- My Progress and the dedicated Developer Tools → Misconception Loop tester share an inspectable observations panel. Saved observations load from an authenticated owner-only endpoint; fictional samples are explicitly opt-in.
- The shared judged runner supplies correction item/phase, expected/observed response and assistance metadata. The existing Next `distillMisconception` LLM action produces a hypothesis plus teaching implication and next-check guidance. Backend persists a bounded packet alongside the existing misconception record. Capture refreshes the panel after confirmed storage.
- The tester pins Grade 4 / compare / medium / `NBT004-01-b`, uses the real chart, ExhibitProvider, LuminaAIProvider and EvaluationProvider, and displays capture outcome and generation adaptation. Automatic submission retries and pending-storage persistence are disabled in this tester.
- A narrowly recognized place-name/value confusion maps to `contrast_place_name_and_value`. Existing deterministic number selection pairs the same nonzero digit across two positions; existing find-place and say-value questions remain intact. Count, mode, support tier, magnitude and scripts are preserved. The wrapper LLM does not receive private diagnosis prose.
- Safe generation metadata reports targeted/already-targeted/insufficient-capacity and saved-observation origin. This is real content adaptation, not just a context preview.
- A separate older digit-worth policy uses backend-owned revision-bound opportunities and first-response evidence. **Only `contrast_digit_worth` can receive that certification. Place-name adaptation cannot resolve a hypothesis through it.** No broad mastery/transfer claim.

## Important incident and evidence limits

The user completed a 67% activity. Backend logs confirmed HTTP 200 and review saves to CosmosDB and Firestore. The runner's 60% aggregate pass threshold suppressed diagnosis evidence, while an unsolved phase made the chart submit success=false. Fixed by retaining correction evidence independently of the aggregate score. Place Value now includes generated problem and evidence in normal `student_work`, independently of LLM success. The mounted regression reproduces phase scores 67/100/67/0/100 and failed before the fix.

Historical logs contain prompts/transcription fragments: place-name responses included a digit identity or its value, while value responses were accepted. That supports a possible name/value confusion, not a definitive diagnosis. Some transcription fragments are noisy; the incorrect written digit layout was not retained in the inspected logs. Do not fabricate a historical structured packet or replay a synthetic score through live calibration.

## Remaining gaps / recommended next three moves

1. **Demonstrate teaching and support fading** — the second producer/consumer is now implemented and machine-verified: Base-Ten read mode reuses the response ledger and consumes a compatible Place Value hypothesis. Next verify the learner experience across the two representations, then implement one deliberate explanation/fading step and test fresh evidence. Do not equate targeted content or successful generator calls with teaching effectiveness or learner transfer.
2. **General evaluation and observation context** — executor `student-data-loop`. The bounded second generation consumer is now Base-Ten Blocks; general evaluator context and strengths/support-driven generation remain open. Grade the current response against its current rubric first; prior hypotheses guide interpretation/coaching and never override a correct answer. Generic payloads in the panel are still previews; live saved-focus delivery is limited to scoped Place Value targeting and its explicit digit-worth bridge to blocks.
3. **Trustworthy joined evidence and human acceptance** — executor `student-data-loop` for submission retry identity/canonical attempt references; user for HUMAN-CHECKS #113/#63. Current observation source references retain client attempt IDs, and generic retries may create new backend attempts. Diagnoses still occupy latest-per-scope slots; new strength/support records accumulate per client attempt with a latest-50 profile projection, without longitudinal reconciliation. Keep the live microphone → stored observation → adapted next activity and teaching effectiveness checks open.

No external blocker to the next machine slice. User acceptance is needed to close human checks; only the user closes those rows. Do not expand resolution policy or sweep all primitives under this handoff.

## Files to start with

Under `my-tutoring-app/src/components/lumina/`:
- `components/{LearningObservationsPanel,SavedLearningObservations,MisconceptionLoopTester,MyProgressPanel}.tsx`
- `evaluation/learningObservations.ts`, `evaluation/learningObservations.fixture.ts`
- `evaluation/diagnosis/{types,distillMisconception,captureMisconception}.ts`
- `evaluation/contexts/EvaluationContext.tsx`, `hooks/useJudgedScriptRunner.ts`
- `service/math/{placeValueRemediation,gemini-place-value,placeValueOpportunityContract}.ts`
- `service/generation/{generationRequest,learningObservationServer}.ts` (catalog-declared consumers; `placeValueOpportunityServer.ts` folded in 2026-09-13)

Backend: `app/api/endpoints/student_profile.py`, `app/db/firestore_service.py`, `app/services/{submission_service,misconception_opportunities}.py`. Canonical submission remains the learning-progress writer. Read the complete student-data-loop skill before changing these paths. Read root instructions, applicable misconception-loop skills and the current report/queue, rather than relying on this list alone.

## Repeatable demonstration and verification

Latest bridge continuation: full frontend **6,009 passed / 10 skipped**, final focused **9 passed**, Lumina typecheck zero, backend **51 passed**. `artifacts/two-primitive-{full-tests,final-focused,typecheck,backend}.log`. Harness: `probe_place_value_authenticated.py --two-primitive-bridge`, run `pvc-http-qa-d562ee3ca0d94c5d8856f08a6ce90e1e`, PASS/cleanup verified. The tester offers “Generate blocks from the same hypothesis.” Old digit-worth chart targeting is evidenced by its issued receipt; blocks use `learningAdaptation`, and receive no resolution receipt. Place-name-only confusion does not bridge.

Latest storage continuation: full frontend **6,002 passed / 10 skipped**, Lumina typecheck zero, five focused backend tests pass. Logs: `artifacts/general-observations-{full-tests,typecheck,backend}.log`. Real authenticated harness `pvc-http-qa-da7b82ddce4e4396b67df3c9698a7339`: PASS for real LLM strength/support → immutable store → owner profile; duplicate saves stable, existing diagnosis unchanged, direct client access denied, cleanup verified. From backend: `python scripts/probe_place_value_authenticated.py --general-observations` using the Python environment below. No synthetic canonical learning submission. Backend was restarted with the same local uvicorn command to load these changes. Review HTML rebuilt with explicit saved-observation evidence; scripts, links, two saved examples and 69 entries checked.

Successful-response continuation: from the absolute frontend directory run `node scripts/probe-learning-response-preview.mjs` against the existing Next listener. Real Next/LLM, fictional evidence, no submission/calibration/store writes. Latest result: `artifacts/learning-response-preview-live.json`, PASS for strength, support, and score-only abstention. Mounted runner regression verifies actual successful text, correction history, and omission of stale/missing transcripts; UI test verifies disabled score-only action and unsaved evidence-linked display. Full frontend suite: **5,999 passed / 10 skipped**; Lumina typecheck zero (`artifacts/learning-responses-{full-tests,typecheck}.log`). No microphone or browser visual acceptance is claimed. New entry files: `components/LearningResponsePreview.tsx`, `evaluation/learningResponseEvidence.ts`, `evaluation/diagnosis/distillLearningObservation.ts` under the Lumina root.

Services: Next on port 3000, backend on 8000. Probe existing listeners; **never start a second Next dev**. Current local ignored env files contain matching signing keys and the frontend backend URL uses `http://127.0.0.1:8000`. Do not print secrets. Backend was started without reload; restart an owned process if backend code changes. No production deployment was performed.

From `C:/Users/xbox3/claude web tutor/backend`:

```powershell
& 'C:/Users/xbox3/miniforge-pypy3/envs/py311env/python.exe' -W ignore scripts/probe_place_value_authenticated.py --observations-only --generation-context
```

The harness uses real Firebase auth, real Next LLM, real Firestore persistence and real generation with fictional evidence. It reserves a disposable student and removes its owned records, Cosmos mapping and Firebase account, verifying absence. It does not submit synthetic answers through canonical learning/calibration. The observations branch returns before the final console print; inspect the newest `authenticated-report.json` under `qa/misconception/place-value-opportunities/` (a successful command may print nothing).

Latest demonstrated run: `pvc-http-qa-1d28be47cc404d9f8db70ec924df4684`. PASS, two targeted draws, baseline unadapted, no resolution credit, cleanup verified. Draw 1 pairs 9 at tens in 6,191 with 9 at hundreds in 8,905. Draw 2 pairs 8 at tens in 1,480 with 8 at hundreds in 4,811. These are historical artifacts; the learner was deleted and receipts cannot be reused.

Latest full frontend suite: **5,994 passed / 10 skipped**, Lumina typecheck zero. Logs: `artifacts/place-name-generation-{full-tests,typecheck,http}.log`. Focused generator contracts: 27 pass. Backend evidence-persistence tests: three pass; earlier opportunity suite: 43 pass. A fixed-seed test proves causal content change and prevents new-move certification. Run commands from the absolute frontend working directory with `npm.cmd test` and `npm.cmd run typecheck:lumina`. Do not claim microphone quality from these tests.

## Review artifact and queues

`artifacts/math-pedagogy-review/index.html` is generated. Edit `progress.html`, `review.template.html`, `plans.json` or `build-review.cjs`, then run `node artifacts/math-pedagogy-review/build-review.cjs` and `node artifacts/math-pedagogy-review/check-review.cjs`. It retains the original 69-entry inventory/proposals and separates the five-stage vision from implemented work. Latest harness evidence is embedded explicitly, not selected nondeterministically from the newest directory. `original-review-2026-09-12.html` preserves the starting artifact.

Added 2026-09-12: `plans.json` holds station-by-station `/add-misconception-loop` build plans for the first six catalog entries (bar-model, number-line, base-ten-blocks, fraction-circles, fraction-bar, place-value-chart), rendered as the `#plans` section, a matrix, and a "Build plan" block on each of those library cards. The build validates each plan against the parsed inventory: unknown primitive, missing station, malformed move id, an "implemented" status without a generator remediation marker (or the reverse), or a named file that does not exist fails the build. Two plans are implemented; four are drafts with nothing built, all gated on the same shared observation-delivery extension. Cheapest next consumer by the plans: fraction-bar identify (fractions and choices are code-built; medium tier guarantees the swap distractor). Layout verified in headless Chromium (`plans-preview.png`, `plans-narrow-preview.png`).

Source of residual work: `qa/di/BACKLOG.md` item 18, indexed in `WORKSTREAMS.md`; human checks #113/#63 remain open. Detailed chronology: `qa/misconception/place-value-opportunities-2026-09-12.md`. There is no need to re-run already passing implementation suites for this documentation handoff alone.
