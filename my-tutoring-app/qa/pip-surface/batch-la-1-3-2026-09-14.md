# Pip shared-surface batches 1–3 (Language Arts) — 2026-09-14

Previous batch: `batch-literacy-2026-09-14.md`. Queue: `ROLLOUT.md`. Where Pip points and never points, per primitive: `src/components/lumina/pip/README.md`.

Fourteen primitives now publish a surface: rhyme-studio, sound-swap, word-workout, phoneme-explorer, decodable-reader (batch 1); story-talk, interactive-book, picture-vocabulary, word-flip, spatial-scene (batch 2); letter-workshop, you-and-me, story-bridge, story-ribbon (batch 3).

## Families

| Family | Primitives | Phase inputs |
| --- | --- | --- |
| Judged runner, spoken | rhyme-studio, phoneme-explorer, decodable-reader, story-talk, you-and-me | runner phases; cue only once `cuedItemId` names the item |
| Judged runner, one committed tap | word-workout (picture match), picture-vocabulary (receptive match), interactive-book (find a part), story-bridge (match, setting, Venn, sequence) | runner + the tapped choice, watched while judged, never received |
| Judged runner, planning board | story-ribbon | runner + the card the child moved; the spoken story is not a handover |
| Speech loop, own stage | sound-swap, word-flip | `onCue` records the item of the last sent cue; the affirmed praise is held as the result until the next cue is sent |
| Classic, synchronous check | spatial-scene, letter-workshop | own check state + `useSpeechScope`; letter-workshop's browser letter-name cue counts as this item's cue |

## Host changes

- `LanguageArtsPrimitivesTester` passes the preview `instanceId` to all 13 literacy primitives here (9 passed none; interactive-book, you-and-me, story-bridge, story-ribbon and letter-workshop passed a fixed `*-tester` id, so the surface and anchor disagreed after regeneration). Spatial Scene's math helper case already passed it.
- `SpatialScene` and its `GridScene` / `PerspectiveScene` take a `sceneRef`; the mode renders sit in one wrapper that records the child's last touch.
- `LetterWorkshop`'s paper `ref` is combined with Pip's through one stable callback.

## Gates

- Vitest: 62 files / 422 tests (all `pip/*`, including the math helper attach test, plus the batch-3 primitives' suites); batches 1–2 suites also run green with their primitives (54 files / 444 and 55 files / 291). 14 new surface tests, 46 tests.
- One policy mutation per primitive: 14/14 caught (6 + 5 + 6 failing tests across the three batches), restored.
- `typecheck:lumina` 0. Full tsc 770 (baseline 770); none in touched files.

## Runtime drives (headless Chromium, :3000 + :8000, signed in, live tutor)

Every drive: `bodies=1 inDock=true anchor=true` before start, a new dock with one body after regeneration, no OVERLAP or PAGE-OVERFLOW-X.

| Primitive | Width | Observed |
| --- | --- | --- |
| rhyme-studio (do they rhyme) | 1400 | live opener → point@pair[region], outline on both cards |
| rhyme-studio (find the rhyme) | 760 | live opener → point@target[region] |
| sound-swap (substitution) | 1400 | live opener → point@word[ring] on "cat"; the connector skirts the tile row; tile tap echo → point@word |
| word-workout (auto) | 1400 | live opener → point@pair[region] |
| phoneme-explorer (sound match) | 1400 | point@stimulus[ring] on the "M" tile; no menu card crossed; card tap echo → point@stimulus |
| phoneme-explorer (sound blend) | 760 | point@sounds[region] |
| decodable-reader (auto) | 1400 | point@line[region] |
| story-talk (listen & tell) | 1400 | point@card[region] through the story read |
| picture-vocabulary (say it) | 1400 | point@stimulus[region] |
| picture-vocabulary (word scale) | 760 | point@stimulus[region] |
| word-flip (add -s) | 1400 | point@frame[region]; source tap echo → point@frame |
| interactive-book (auto) | 1400 | find a part → point@page[region]; part tap → checking/look; live affirmation → celebrating; next read item → point@glow[region], outline on "nest" |
| spatial-scene (auto) | 1400, 760 | live tutor → point@scene[region] around the grid; dock between instruction and grid |
| letter-workshop (auto) | 1400 | live tutor audio → point@paper[region]; drawing → working/look |
| story-bridge (auto) | 1400 | live opener → point@anchor[ring] on the ringed friend; the connector crosses the story title only; choice tap → checking/look (no verdict within 45s: the tutor was still reading the stories) |
| story-bridge (auto) | 760 | point@anchor[ring]; this generation put the friend in the second story and the connector ran just past the first story's choice cards — fixed, see below; rerun with a first-story friend → point@anchor |
| story-ribbon (auto) | 1400, 760 | live opener → point@ribbon[region]; card tap → working/look |
| you-and-me (auto) | 1400 | live opener → point@scene[region] |
| letter-workshop (auto) | 760 | live tutor audio → point@paper[region]; drawing → working/look |

## Fixes during the work

- **Story Bridge connector past the choices (760px):** the story panels stack below `md`, so when the ringed friend is in the second story the choice cards sit between it and the dock. Pip now points at the friend only when it is in the first story and outlines both stories otherwise; a surface test covers the flipped pair and fails when the rule is removed.
- **Test harness trap:** `return { store, rerender, ...view }` let RTL's own `rerender` overwrite the helper and unmount the tree, which read as "Pip lost its surface". Spread the view first.
- **Drive script:** `start` only matched "Tap to start"; you-and-me, story-bridge and story-ribbon use custom idle labels. `pipdrive.mjs` and `didrive.mjs` accept `start:<label>`; a loose label ("talk") matches the helper's "Story Talk" entry, so pass the exact text.

## Findings queued (EVAL_TRACKER, executor `/eval-fix`)

Pre-existing, same family as CVCS-1/PBL-1; both probed on the rendered component.

- **SWAP-2 MEDIUM** — after one affirmation sound-swap leaves `stage` at `affirmed`: every later item reads "yes!", the substitution sound-to-change highlight (easy/medium tiers) never shows again, and the reward word never prints except on the last item.
- **WFLIP-1 LOW** — word-flip: "yes!" instead of "two what?" after one affirmation; the affirmed word never prints except on the last item.

## Not verified

- Story Bridge with the friend in the second story was not driven after the fix (generation chose the first story); the rule is covered by its surface test.
- `celebrating` after a real affirmation was seen live only on interactive-book; elsewhere it comes from phase tests.
- Lesson host (scroll-focus claim), reduced motion, mouth timing against real speech.
- Spatial Scene's `describe_scene` beat (its own runner inside the classic primitive) was not driven; its speech counts through the beat's instance id `${instanceId}-${challenge.id}`.
