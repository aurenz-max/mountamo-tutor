# Pip shared-surface batch 4 (K spoken remainder) — 2026-09-15

Queue: `ROLLOUT.md` (batch 4). Previous: `batch-la-1-3-2026-09-14.md`. Per-primitive targets: `src/components/lumina/pip/README.md`.

| Primitive | Family | Points at | Never points at |
| --- | --- | --- | --- |
| letter-sound-link | judged runner; hear-see is one committed tap | the letter card (see-hear, keyword match); the letter buttons as one region (hear-see) | one letter button; a keyword picture |
| syllable-clapper | judged runner, spoken | the hear-it-again button | the reveal bar |
| knowledge-check | judged runner; choice_tap and point_to are taps | the question card as a whole | a choice, True/False card, sort group, word-bank word, number-sentence token |
| di-letter-sounds | speech loop, no reward beat (DI Word Reading's latch) | the stage | the word's first letter |
| di-sentence-reading | speech loop with a reward beat (DI Math Facts' shape) | the whole sentence | one word |

## Host changes

- `KnowledgeCheckTester` had no companion and no instance id: Pip could not appear. It now mounts `CuratorCompanion` beside the check and passes `kc-tester-<generation>`.
- `LanguageArtsPrimitivesTester` passes the preview `instanceId` to letter-sound-link and syllable-clapper. The DI Lab already passed one.
- Knowledge Check's no-microphone tap flow (`KnowledgeCheckTapFlow`) publishes no surface and keeps the perch.

## Gates

- Vitest: 5 new surface tests (14 tests); all `pip/*` 58 files / 189 tests; the five primitives' own suites plus the whole DI folder 39 files / 528 tests.
- One policy mutation per primitive: 5/5 caught, restored.
- `typecheck:lumina` 0. Full tsc 770 (baseline 770).

## Drives

Headless Chromium, :3000 + :8000, signed in, live tutor. Every drive that reached a surface: `bodies=1 inDock=true` before start, a new dock with one body after regeneration, no OVERLAP or PAGE-OVERFLOW-X.

| Primitive | Width | Observed |
| --- | --- | --- |
| letter-sound-link (auto; first item see-hear) | 1400 | live opener → point@letter[region] |
| syllable-clapper (auto) | 1400, 760 | live opener → point@stimulus[region] |
| di-letter-sounds (letter_sound) | 1400 | live opener → point@stage[region] |
| di-sentence-reading (decodable_sentence) | 1400, 760 | live opener → point@sentence[region] |
| knowledge-check (tester) | 1400 | **not reached.** Both generations rendered the tap flow (a sequencing activity; then a set the judged build did not accept), so Pip stayed on its perch as designed. The judged surface is verified by its phase tests only. |

## Not verified

- Hear-see letter taps and a real affirmation on any of the five; phase tests only.
- Knowledge Check's judged surface in a browser (above). A lesson that routes a judged-viable set would show it.
- Lesson host, reduced motion, mouth timing against real speech.

## Harness

`pipdrive.mjs` now runs single-primitive testers: `PIP_HELPER="Knowledge Check"` with `-` as the label, its "Orchestrate & Preview" button counts as generate, and `PIP_PRECLICKS="Fractions;Recall"` presses buttons (a topic, a tier) before generating.
