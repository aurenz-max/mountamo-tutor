# Birth Certificate — era-explorer (2026-08-23)

**Lifecycle layer: L5 (judged, 09-03)** — born L0 (sound, measurable, single core mode, generic tutor); raised to L1 by `/add-eval-modes`, to L2 by `/add-tutoring-scaffold`, to L3 by `/add-support-tiers`, to L4 by `/add-structural-difficulty`, to **L5 by `/add-sound` + `/add-di-loop`** — the tutor now owns the clock and every answer is spoken (port 24, the FIRST history judged loop). Everything below the line describes the CLICK ERA; where the port deleted a surface, queue items 5 and 6 say what replaced it.
**First primitive of the history domain** (PRD_HISTORY_SOCIAL_STUDIES_SUITE #3, Wave 1) — this birth also created the domain scaffolding every later history primitive rides on: `catalog/history.ts`, `service/history/`, `historyGenerators.ts`, `history → SOCIAL_STUDIES` backend scoping.

- Core task identity at birth: `era_sort` (now the β3.5 anchor rung of a 4-mode ladder — see queue item 1) — explore a 3-lens era card (Daily Life / Technology / School & Work), then classify life-detail statements into **{era} / Today / Both then and now** (era characteristics + continuity-vs-change, the C3 anchor)
- Generator fork: B (content-bearing), **single-call era-coherent variant** — one Gemini call emits era card + bounded challenges[4..6]; N parallel calls would fragment the era. Model: gemini-flash-lite-latest, shallow schema, retry-once → curated Pioneer Times fallback
- sendText tags wired: [ACTIVITY_START], [LENS_OPENED], [CHALLENGES_START], [ANSWER_CORRECT], [ANSWER_INCORRECT], [NEXT_ITEM], [ALL_COMPLETE]; added at L2: [ERA_READ_ALOUD], [ERA_FIGURE_VOICE]
- Answer-leak audit: statements code-gated against time words/years/era-name words (TIME_LEAK regex + era-word check) AND against verbatim lens copying (≥7-word-run tripwire); explanation renders post-answer only; hint names a lens, never a bin; no per-bin tallies; lens card = legitimate open-book stimulus by design
- Design gate (Phase 2):
  1. Direct manipulation — pass: the statement→time-bin placement IS the learning object (tap-to-place classification)
  2. Living simulation — **chosen exception**: era analysis is a stimulus-and-judgment primitive (FactFile archetype); the visit-gated lens card is the explorable surface; no physical system to simulate
  3. Production over recognition — pass with roadmap: 3-way judgment incl. the "Both" continuity bin demands inference (verbatim tripwire enforces it); full spoken production (explain WHY the era was that way) is the documented L5 rung per the PRD modality doctrine
  4. No timers — pass: none rendered; elapsedMs captured silently
  5. No answer-leak by layout — pass: audited above, enforced in generator code
- Curriculum home: **MATCH at every published SOCIAL_STUDIES grade** (K 0.754, G1 0.766, G2 0.775, G3 0.800, G4 0.745 — see qa/curriculum-fit/era-explorer-2026-08-23.md). Scoping fix shipped: added `"history": "SOCIAL_STUDIES"` to `_DOMAIN_TO_SUBJECT` (was absent — new domain)

## Follow-up queue (run in order — each skill is the single source of truth for its layer)

| # | Skill | Layer | Input from this birth |
|---|-------|-------|----------------------|
| ✅ 1 | `/add-eval-modes` | **L1 eval-dense — CLOSED 08-23** | Shipped 4 rungs: `lens_id` β2.0 / `era_sort` β3.5 (anchor) / `era_compare` β5.0 / `cause_of_change` β6.5, backend β + a/c priors matched. `figure_era` DECLINED — "which era is this person from" is an `era_sort` statement with a person as subject, and role-differentiated experience belongs to `perspective-lens` (PRD line 755). Structural changes: challenge type moved to `challenges[].type` (root enum would have made mixed a lie, SP-21); `priorEra {name, body}` added as the era_compare stimulus; Gemini emits answer TEXT, code owns options + `correctIndex`; per-mode leak audit (lens-title / time-word / causal-connective / cause-echo). Fixed en route: the summary panel replaced the LAST challenge the moment its answer was recorded, so the final explanation was never readable — now gated on advancing past it |
| ✅ 2 | `/add-tutoring-scaffold` | **L2 tutored — CLOSED 08-23** | Catalog `tutoring:` block shipped: 14 contextKeys (all component-resolved), 3 scaffolding levels written band-neutral so they never ask a pre-reader to read, 6 common struggles (presentism, "both" avoidance, compare-against-today, cause-vs-change, never-opened-a-lens, non-reader), 4 aiDirectives — **NEVER NAME THE BOX** (the 3 choices ARE the answer; the tutor points at a lens or at the student's own day), **THE FOUR HISTORIAN MOVES** (per-`challengeType` nudge method), **PRE-READER READ-ALOUD**, **VOICE OF SOMEONE WHO LIVED THEN**. Component gained `isPreReader` (K/1), `readAloud()` → `[ERA_READ_ALOUD]` behind `LuminaReadAloud` on the lens body AND on the challenge prompt (reads question + statement + all three choices, `lg` tap tier at PRE), and `figureVoice()` → `[ERA_FIGURE_VOICE]` on an explore-phase-only button — gated there so a first-person voice can never brush a live statement. Bag grew `gradeLevel`, `activeLens`, `question`. Gates: `typecheck:lumina` 0; `/tutor-test era-explorer` Tier 1 PASS 0 findings; Tier 2 probe (era_sort @ K, real generation) PASS, 0 literal `(not set)`, every var `resolvedBy: component`. Caught by the probe: `commonStruggles.pattern` is NOT interpolated by the backend — a `{{key}}` there reaches Gemini literally; reworded. **Not yet exercised live** (Tier 3 / Lumina Tutor Tester): no ear on the read-aloud voice or the era persona |
| ✅ 3 | `/add-support-tiers` | **L3 tiered — CLOSED 08-23** | Six levers, all three birth candidates shipped plus two the read of the component found. Per challenge (from that challenge's OWN `type`, so blends tier too): `showStrategy` — the historian move NAMED under the question at easy only (instruction-as-scaffold, the strongest family for this archetype; copy lives in `MODE_META.strategy`, code-built, never LLM-authored); `hintLevel` named_lens → generic → none, with `lens_id` **hard-guarded** off the named rung at every tier (the lens IS its answer); `showBinCaptions` — "Long ago / Our time / Then AND now" gone at hard, leaving bare labels. Per session: `requireAllLenses` — the explore gate + its counter relax at hard (choosing what to consult becomes the task); `lensAccess` — the lens + priorEra cards fold away between challenges at hard, one tap from the student and **re-folded on every advance** so one tap can't downgrade the rest of the session. Gotcha #2 closed: `tutorRevealPolicy(tier)` on `[ACTIVITY_START]`/`[CHALLENGES_START]` + a tier branch on the `[ANSWER_INCORRECT]` nudge, which stops naming `lensHint` at hard — the tier hid the lens on screen, so the tutor may not hand it back. Answer-bearing check: **none** — no lever writes a field a checker reads; `options`/`correctIndex` byte-identical at every tier. DECLINED as non-levers: read-aloud (baseline access, never a hint), the bin **icons** (a pre-reader's only channel to a choice — withdrawing them would be a wrong-band move), `figureVoice` (a designed experience), `MAX_TRIES` (changes the score, not the scaffold). Gates: `typecheck:lumina` 0; 11/11 vitest across 2 files incl. a new 6-test L3 render gate, **mutation-checked** (breaking the caption gate and the tutor branch each fails a test, so the assertions aren't vacuous); 9 live generations through `/api/lumina/eval-test?difficulty=` (era_sort @ G3, lens_id @ K, mixed @ G4 × easy/med/hard) all `status: pass` with the tier fields correct per challenge — in the BLENDED run `lens_id` took `generic` while the other three took `named_lens` in the same session, which is the per-challenge rule working. **Not browser-driven**: the render tree is exercised in jsdom, not Chrome |
| ✅ 4 | `/add-structural-difficulty` | **L4 shaped — CLOSED 08-24** | Four in-mode levers, generator-only. Because statements are PROSE and cannot be reconstructed in code, enforcement is **over-generate → measure → select**: under a tier the schema asks for a deeper pool (challenges 4-6 → 6-9, cause distractors 2 → 4), `selectForShape` measures each candidate and ships the set that hits the target — both widenings gated on `supportTier`, so the no-tier path stays byte-identical. Levers: `era_sort`/`era_compare` **subtle-bin share** (1 → 2 → 3 of 6 on the "both" continuity bin) with the birth's artifact → practice → institution subject ladder as the prompt half; `lens_id` **cross-lens reach**; `cause_of_change` **distractor distance** (2 farthest → 2 nearest of 4, near-duplicates excluded so there is always exactly one right answer). `TIER_GUARDRAIL` reworded — structure changes, magnitude and READING LEVEL do not. Gates: tsc at baseline 802/0 new, `typecheck:lumina` 0, 35/35 vitest incl. a new 24-test stress suite over 6,000 seeded random pools, mutation-checked ×3. 13 live generations all `status: pass`; era_sort @ G3 hit **1/2/3 of 6 exactly**. **Caught live:** the hard `cause_of_change` prompt said "the SAME period", which pulled statements off the mode into era descriptions — reworded and re-driven. Residuals in `qa/eval-reports/era-explorer-structural-difficulty-2026-08-24.md`: `lens_id`'s ceiling is only PARTIAL (the lexical reach proxy saturates when a paraphrase shares little vocabulary with any lens — one hard run shipped 0/6 on target and said so), plus mild sentence-length drift at hard |
| ✅ 5 | `/add-sound` | **L5 — CLOSED 09-03** | Three SEMANTIC corrections, no new points: the four the birth wired were already there and the wrong sound sat on each event. Bin-select is a CHOICE → `select()` (it was a neutral `tap`, so the bin row sounded like more browsing); a FIRST lens open reveals a card → `pop()` while a revisit stays `tap()`, so the ear and the explore counter agree about what progressed; the source disclosure is a toggle → `toggle(on)`. Completion fanfare DECLINED as a non-lever — `CelebrationLayer` owns it on `submitResult` and a manual `playPerfect()` would collide with it. Structurally small, and paired with item 6 for exactly that reason. After the port the bin sounds shipped OUT with the bins, so `select()` survives on nothing and FOUR points ship: `pop` / `tap` on the lens tabs, `toggle(on)` on the source disclosure, and `playCorrect()` moved off the deleted Check press onto the REVEAL. No `playIncorrect` counterpart on purpose — a wrong answer is answered by the tutor's correction, and a sound under it would talk over the teaching |
| ✅ 6 | `/add-di-loop` | **L5 STRONG FORM — CLOSED 09-03** | `/add-voice-control` was SKIPPED, not done: the user pulled the strong form directly, and the interim rung would have been thrown away by it. Port 24, the first history / social-studies port. **All four modes spoken, zero taps on the answer**, all `closed_set_choice` — the uniformity is ARGUED rather than assumed, because this is a three-bin classification primitive in every one of its modes, so the menu is the ask and the BUTTON is what the port deletes. Deleted: the three bin buttons, Check, Next, the explore gate, the hint ladder, the `MAX_TRIES` reveal, `[ERA_FIGURE_VOICE]`, and the pre-affirm explanation. KEPT: the era card — this primitive is open-book by design, so the card is the PAGE and not apparatus, and `sourceCue` reads a lens body aloud for a pre-reader. The L3 tier levers moved into the spoken lead-in. Gates: `typecheck:lumina` 0, tsc at the 802 baseline, vitest 4480/0, 34 pins mutation-checked ×7, 6 live draws with clean pack gates, **4 headless DI drives PASS — 34/34 wrongs refused, every signature miss included**. Mic **#127**. Full block: `qa/di/BACKLOG.md` item 29 |
| ✓ | `/eval-test era-explorer` | QA loop | Birth: 4 live runs, all PASS. **L4: 13 more live runs, all PASS** (era_sort × easy/med/hard @ G3, lens_id × easy/hard @ G3, cause_of_change × easy/hard @ G5, era_compare × easy/hard @ G4, plus a no-tier baseline confirming the untiered path). L1: 6 more live runs — all 4 modes pinned (each isolated to its own type) + mixed at K and G5 (grade steer honored: no `cause_of_change` at K, no `lens_id` at G5). Re-run after EVERY layer |

**Content note from the L3 tier sweep (for `/eval-test`, not a tier bug):** an era_sort hard run keyed "Workers dig deep ditches to guide river water into dry farming fields" as *Ancient Egypt only*, while the easy run keyed the same irrigation idea as *Both then and now*. Modern irrigation makes "both" defensible — the generator's own "both" guidance may need a line about practices that survived in changed form.

**Domain follow-on (not this primitive's queue):** next Wave-1 births per the PRD — `source-detective` (signature discipline skill; extends interactive-passage highlighting), `cause-effect-chain` (nested-hierarchy graph pattern), `map-lab` (needs the map-canvas spike, PRD open question 1).
