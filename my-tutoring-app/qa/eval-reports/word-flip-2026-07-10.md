# Eval Report — word-flip (2026-07-10)

**Primitive:** `word-flip` (L0 birth, single mode `plural_s`)
**Component:** `src/components/lumina/primitives/visual-primitives/literacy/WordFlip.tsx`
**Generator:** `src/components/lumina/service/literacy/gemini-word-flip.ts`
**Endpoint:** `GET /api/lumina/eval-test?componentId=word-flip&evalMode=plural_s` (3 live runs)

## Results

| Run | API Status | Challenges | G1 | G2 | G3 | G4 | G5 | Verdict |
|-----|-----------|------------|----|----|----|----|----|---------|
| 1 | pass (1414ms) | 5 | PASS | PASS | N/A | PASS | PASS | PASS |
| 2 | pass (1324ms) | 5 | PASS | PASS | N/A | PASS | PASS | PASS |
| 3 | pass (1371ms) | 5 | PASS | PASS | N/A | PASS | PASS | PASS |

**Overall verdict: PASS (3/3 runs, 15/15 challenges clean)**

## Per-run detail

| Run | Title | Singulars | Counts |
|-----|-------|-----------|--------|
| 1 | Many More Wonders: A Counting Adventure! | moon, cat, cloud, hat, tree | 4, 2, 5, 2, 3 |
| 2 | Fun With Many: The Counting Game! | duck, balloon, star, spoon, apple | 5, 2, 4, 3, 3 |
| 3 | Fun With Many: A Number Counting Game | boat, plate, lamp, cat, hat | 2, 2, 5, 4, 3 |

## Gate detail

- **G1 (required fields):** Every challenge: `type: 'plural_s'`; singular matched `/^[a-z]{2,12}$/`; `answer === singular + 's'` exactly; emoji = one real non-ASCII emoji; count integer 2–5; options = exactly 3 lowercase strings = {answer, singular, over-regularized `-ses`} (set-equality checked); ids `word-flip-1..5` unique and ordinal. Session title/description non-empty, `challengeType: 'plural_s'`, exactly 5 challenges. 15/15 challenges pass on every check.
- **G2 (options populated):** 3 non-empty strings on 100% of challenges (15/15). No flat indexed fields exist in this schema — nothing to reconstruct.
- **G3:** N/A at L0 birth.
- **G4 (answer derivability):** answer present in options on 15/15; answer derivable from visible singular via the -s rule (answer is code-derived, so this holds by construction). Plural-guard leak check: zero singulars ending in s/x/z/ch/sh/o, f/fe, or consonant+y; zero irregulars (checked against the full 17-word list). 0 leaks across 15 challenges.
- **G5 (fallback quality):** All `??`/`||`/ternary fallbacks audited: `raw.nouns ?? []`, `word?/emoji? ?? ''`, `title/description || ''` all funnel into reject-or-throw paths (`pool.length < 5` throws after one corrective retry; empty title/description throws). The keep-better-attempt ternary is benign. No fallback can silently fabricate or corrupt a challenge — answers and all distractors are derived in code from the validated singular, so stimulus/answer desync is structurally impossible.

## Variance

- **Noun entropy (across runs):** pairwise overlap 1v2 = 0, 1v3 = 2 (cat, hat), 2v3 = 0; 13 distinct nouns across 15 slots. Seed-shuffle entropy injection is working — no converged noun set. Run 2 and 3 produced nouns outside the seed list (balloon, plate, lamp), confirming live model contribution, not seed-echo.
- **Count variety (within runs):** variety = 4 distinct counts in every run (the shuffled one-of-each + one-repeat construction guarantees ≥4). No monoculture.

## Issues found / fixed

- **Harness usage, not a generator bug:** `?componentId=word-flip` alone returns the full catalog (the route requires `componentId` AND `evalMode` for single-test mode — `src/app/api/lumina/eval-test/route.ts:20`). Re-ran with `&evalMode=plural_s`. No generator or component changes were needed.
- Route note: word-flip has no `evalModes` entry in the catalog yet (expected at L0), so the route's challenge-type validation is skipped ("No eval mode in catalog"); the gate checks above were run externally against the raw `fullData`.

No fixes required. Generator ships clean at L0.
