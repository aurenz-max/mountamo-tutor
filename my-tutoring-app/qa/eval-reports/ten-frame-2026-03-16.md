# Eval Report: ten-frame — 2026-03-16

## Results

| Eval Mode | Status | Issues |
|-----------|--------|--------|
| build     | PASS   | —      |
| subitize  | PASS   | —      |
| make_ten  | PASS   | —      |
| operate   | PASS   | —      |

## Notes

### TF-1 (make_ten — answer leak) — FIXED
Prompt updated: `showEmptyCount: false for make_ten` (previously told Gemini to set it true, which displayed the complement answer). Gemini now generates `showEmptyCount: false` for make_ten mode.

### TF-2 (operate — misleading add instruction) — FIXED
`add` promptDoc rewritten to clarify: frame starts EMPTY, student places both addends. Instruction must say "Place X, then add Y more" — not "You already have X counters." Gemini now generates correct add instructions.
