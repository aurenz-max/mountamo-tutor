# Eval Report: ramp-lab — 2026-08-21

## Results

| Eval Mode | Status | Issues |
|-----------|--------|--------|
| `compare_conditions` | PASS | — |
| `find_threshold` | PASS | — |
| `design_with_budget` | PASS | — |

The running app generated four code-owned challenges for every explicit pin, with only
the requested task identity present. The explicit `mixed` path returned six challenges
in a `compare → threshold → design` rotation, covering all three modes before repeating.
An unpinned objective about measuring minimum force resolved to `find_threshold` and
returned its four threshold challenges.

The seven-case content contract also passes: matched comparisons change only the named
causal variable, every threshold answer is the first valid 0.5 N slider setting, every
design has one unique steepest whole-degree solution, and catalog/pool keys agree.

Focused Lumina typecheck: **0 errors**.

Headless Chrome rendered the design mode at both 10° and 50°: the platform height stayed
fixed, the ramp shortened as angle increased, and the force evidence remained hidden before
checking. Browser-driven checks exercised all three result branches: workable-but-not-
steepest, over-budget, and the exact 24° solution.

## Visual check remaining

Open Engineering Primitives Tester, select `ramp-lab`, and visually exercise one challenge
from each mode. The dedicated design-mode browser probe is green; this remaining user check
covers the real tester selector plus the compare and threshold interaction feel.
