# Workstreams — Portfolio Index

Start here for "what's next?", then pull the top item of an ACTIVE stream's queue.
**Queues are authority over this file; this file over memory.**

## ⚖️ Rules

This file holds **state** — not findings, evidence, or reasoning. Run the three gates
before you finish:

```bash
wc -c < WORKSTREAMS.md                                            # ≤ 10000 BYTES
awk 'length>400 && /^\|/ {print FNR": "length}' WORKSTREAMS.md    # empty (whole LINE, bytes)
grep -c '^> ### ' WORKSTREAMS.md                                  # 0 — /pm edits rows, never appends
```

It was split three times in four days and regrew each time — because findings were written
here instead of where their reader acts. Decide the home at WRITE time:

| The finding… | Home |
|---|---|
| changes how the next slice is **DONE** | the executor skill (`/add-di-loop`, `/reader-fit`, …) |
| is true about **ONE primitive** | `docs/contracts/<id>.md` |
| is a **defect CLASS** | its own queue item |
| is **evidence of a run** | `qa/tutor-reports/`, `qa/<lane>/` |
| is **state** (lane, health, next pull) | **here — as a row** |

**States:** ACTIVE = worked now · DELEGATED = another session holds it, read its report
first · PARKED = idle, queue trusted only as of its date · BLOCKED = named dependency.
Prior snapshots and the reasoning behind past calls: `git log -p WORKSTREAMS.md`.

## Portfolio — as of 2026-09-04

**WIP 2 ACTIVE + 1.** Judged-loop ports are ONE stream (`/add-di-loop`, queue
`qa/di/BACKLOG.md`); science / math / picture-vocabulary are its items, not rival lanes.

| Lane | State | Pull now | As of |
|---|---|---|---|
| ↳ **Place-value misconception pilot** | IN PROGRESS: shared LLM applicability verified | Shared LLM applicability replaces generation regex. Place Value first, then Blocks: 14 real positive/paraphrase/abstain cases passed; fresh authenticated one-source/two-primitive run passed. Both emit shared response evidence; real LLM/auth/store/generation harness passes for two draws each, cleanup verified. Review embeds contrasts. Next: live teaching/fading acceptance, general context delivery and canonical retry identity. No cross-primitive resolution; #113/#63 open. Review now carries station-by-station build plans for the first six catalog entries (`artifacts/math-pedagogy-review/plans.json`; fraction-bar SHIPPED 09-13 as a `build` consumer — identify cannot host the move — plus the shared source-agnostic delivery path and a signing-key fix; [report](my-tutoring-app/qa/misconception/fraction-bar-2026-09-13.md); bar-model and number-line in flight in parallel sessions). **09-13 fraction-circles compare IMPLEMENTED** (capture repair via `firstResponseScore` — compare always scored 100 — plus consumer `contrast_same_numerator_denominators` at five reviewed G3/G4 subskills; distiller, planner 27/27, authenticated PASS; queued in item 18: Grade 3 denominator scope `/topic-fidelity`, touch capture unprobed; #158) — [report](my-tutoring-app/qa/misconception/fraction-circles-2026-09-13.md). **09-13 bar-model picture_graph IMPLEMENTED** (capture repair + consumer, Grade 3 MEAS003-04-a; 14/14 real draws + authenticated PASS; residual axis-leak/variety in BACKLOG item 18) — [report](my-tutoring-app/qa/misconception/bar-model-picture-graph-2026-09-13.md). **09-13 base-ten-blocks read_blocks is now an observation SOURCE** (catalog skill scope, factual correction evidence, `SERVER_DELIVERED_SOURCES` capture/resolution policy, shared delivery, code-owned grade band; distiller 5/5, planner 12/12, authenticated PASS; mic #157; first-response gate adopted; queued in item 18: USER RULING on chart receiving blocks-origin observations) — [report](my-tutoring-app/qa/misconception/base-ten-blocks-origin-2026-09-13.md). **09-13 number-line jump IMPLEMENTED** (capture repair: exact-landing grading, the shared opt-in `firstResponseScore` gate now used by four consumers, hint leak NL-3 fixed; consumer `contrast_start_positions` at Grade 1 OPS001-03-a/OPS001-04-a; distiller, planner 6/6, generation 41/41, authenticated PASS; queued in item 18: NL-2 plot tolerance `/eval-fix`, `/misconception-test`; #159) — [report](my-tutoring-app/qa/misconception/number-line-jump-2026-09-13.md). **09-13 backend primitive-agnostic (USER RULING)**: no primitive names or tables in production backend code; catalog `observationDelivery: 'server'` relayed on capture, server delivery = stamped `scope_context` record property, generic receipts judge against the plan's `accepted_answers`; 76/76 misconception tests, full suite unchanged vs HEAD, typecheck 0, authenticated probe PASS. **09-13 frontend half CLOSED**: catalog `learningObservations: { eligible, retest? }` declaration + one generic branch in `geminiService.ts`; scope subject from `config.objectiveSubject`; place-value retest is a catalog `retest` declaration; typecheck 0, vitest 95/95, authenticated probes PASS with cleanup (fraction-bar, place-value, number-line) — [report](my-tutoring-app/qa/misconception/catalog-observation-consumers-2026-09-13.md). **09-13 area-model IMPLEMENTED** (capture repair — one wrong cell per model still scored 100 — plus consumer via the catalog declaration: `contrast_same_fact_across_places` for build_model/find_area/multiply, `contrast_equal_area_perimeters` for perimeter; distiller 4/4, planner 10/10, generation 30/31 (Gemini 503), authenticated PASS; queued in item 18: AM-3/4/5 `/eval-fix`, `/misconception-test`, `/topic-fidelity` one-digit homes; #160) — [report](my-tutoring-app/qa/misconception/area-model-2026-09-13.md). **09-14 number-tracer sequence IMPLEMENTED** (grader repaired first via `/eval-fix`: blind vision, geometry accepting wrong numerals, answer guide painted, repeated answers, runaway window call; consumer `contrast_gap_positions_in_one_run` at K/G1; distiller 5/5, planner 14/14, generation 24/24, authenticated PASS; #161; queued in item 18: NT-9..NT-12, `/misconception-test`) — [report](my-tutoring-app/qa/misconception/number-tracer-2026-09-14.md). **09-14 counting-board take_away/add_more/count_on IMPLEMENTED** (capture repair: boards misdescribed as "count N", `firstResponseScore` — three wrong first answers still scored 80; moves `contrast_same_start_different_change` + `count_on_exactly_one_more`; distiller 8/8, planner 24/24, generation 67/67, signed-packet replay PASS; #163; queued in item 18: `/misconception-test`; CNB-2/CNB-3 RESOLVED 09-14 via `/eval-fix` (compare larger group on either side, catalog eval modes submitted), compare move now buildable, CNB-4/CNB-5 filed) — [report](my-tutoring-app/qa/misconception/counting-board-2026-09-14.md). **09-14 ten-frame operate IMPLEMENTED** (capture repair on all eight types: voice items misdescribed, `firstResponseScore` — three wrong first answers still scored 80, evalMode `add`/`split` → catalog `operate`/`decompose` (TF-6); move `contrast_same_first_number_different_second` K–2; judge contradiction on subtraction doubles fixed, keep-doubles question TF-5 `/eval-fix`; distiller 8/8, planner 20/20, generation 33/33, signed-packet replay PASS; #164; queued in item 18: `/misconception-test`) — [report](my-tutoring-app/qa/misconception/ten-frame-2026-09-14.md). **09-14 judged evidence RUNNER-OWNED (handoff slice 1 DONE)**: `hooks/judgedRunEvidence.ts` sets `firstResponseScore` and keeps every item's first wrong attempt for all 58 runner components; counting-board/ten-frame builders + base-ten patch deleted, six more callers submit; census over 23 declared sources (real generation → mounted pack → real distiller): gate fires 20/20, 14 hypotheses name the signature miss, 4 capture gaps queued (ordinal-line, decodable-reader, 3d-shape-explorer, picture-vocabulary) — [report](my-tutoring-app/qa/misconception/judged-evidence-census-2026-09-14.md). **09-15 handoff slices 2–4 DONE**: one pack `observation` callback (four packs migrated, 71 on a deprecated alias), catalog eval mode normalised at the evaluation boundary (`evalModeKey.ts`, table-tested over the catalog), one generator adaptation step (`adaptationStep.ts`, all ten consumers ported, fences unchanged, ten-frame signed-packet replay 2/2) — [report](my-tutoring-app/qa/misconception/judged-evidence-slices-2-4-2026-09-15.md). **09-15 capture repairs DONE** for the four census abstains (line order, story sentence, solid geometry, picture named; all four on the one `observation` callback): ordinal-line now distils "counts from the back of the line", the other three abstain on the harness fixture, not on missing facts — [report](my-tutoring-app/qa/misconception/judged-capture-repairs-2026-09-15.md). **09-15 alias migration Phase A (math) DONE**: 11 packs on `observation`, stimulus stated, and `learningResponses` now actually submitted (the runner collected them but most hosts never put them in student work); census no regressions vs run2 — [report](my-tutoring-app/qa/misconception/observation-alias-phase-a-math-2026-09-15.md). **09-15 Phase B (literacy) DONE**: 18 packs, 7/7 census hypotheses hold, letter-spotter confusion pairs now corrections-only — [report](my-tutoring-app/qa/misconception/observation-alias-phase-b-literacy-2026-09-15.md). **09-15 Phase C (DI) DONE**: 5 packs, 4/4 census hypotheses hold, word-problem placements fully described — [report](my-tutoring-app/qa/misconception/observation-alias-phase-c-di-2026-09-15.md). **09-15 Phase D (other) DONE**: last 11 packs, no pack left on the alias; gesture observations now record the move — [report](my-tutoring-app/qa/misconception/observation-alias-phase-d-other-2026-09-15.md). **Next in this lane:** Phase E, delete the alias, full census rerun ([handoff](my-tutoring-app/qa/HANDOFF-observation-alias-migration-2026-09-15.md)); A–D uncommitted. **09-14 harness handoff revised**: no-login delivery replay piloted, per-primitive authenticated probes to be retired ([handoff](my-tutoring-app/qa/HANDOFF-misconception-probe-harness-2026-09-13.md)). **09-14 delivery = SIGNED LAUNCH PACKET (user direction: frontend pipeline, backend storage-only)**: `/generation-context` signs the learner's observations once; generation makes NO backend call; two signed read routes + six per-primitive authenticated probes deleted; replay needs no login and no backend; generic smoke PASS; #162 — [report](my-tutoring-app/qa/misconception/launch-packet-delivery-2026-09-14.md). Still queued in item 18: the primitive-specific backend modules (IRT prior registry etc.) as separate slices; USER RULING on blocks-origin → chart; USER RULING on moving retest receipt issuance off the generation path; shared probe harness runner/fixtures ([handoff](my-tutoring-app/qa/HANDOFF-misconception-probe-harness-2026-09-13.md)). [Current handoff](my-tutoring-app/qa/HANDOFF-learning-observations-2026-09-12.md). | 09-15 |
| 🚀 **PROD / `main`** | ✅ **LEVEL at `2ec88137`** | Shipped 09-13 in **4 slices**: learning-observation framework + 6-primitive sweep, `reading-repair-studio` L0-L2+L5 birth, a small auth-guard follow-up, `curriculum-design-review` skill. Gates: typecheck:lumina 0, frontend vitest 6132/0, backend pytest 65/65 in touched files. **Owed: mic sittings #125–#130** from the 09-04 ship — `di-dice-roll` reached `main` with NO live drive. | 09-13 |
| 🔝 **JUDGED-LOOP PORTS** | **ACTIVE** — `qa/di/BACKLOG.md` | Shipped: 18 math port, 31, **34**, **36 ✅ 09-07** (`explain_concept` — `concept_statement` BENCHED 0/32 false affirms, paraphrase 8/8; 6/6 fresh explain objectives `SUFFICIENT` when the block ships; `DI_PORTS` adapter closes #137's no-drive; mic #139; residual = fresh-draw yield ~60%, queued). Open: **33** `poetry-lab[rhyme_hunt]`; 30 owns 2 cap WARNs (+ a third witness: the ten-rod sitting's off-script drift). **35 RETRACTED**. **40 ✅ 09-12** — `syllable-clapper` three ACTS (`blend_syllables` β1.5 · `count_parts` β2.5 · `delete_compound` β3.5) where there were three word LENGTHS; length moved to the tier, `metrics.evalMode` fixed (attempts had all filed as `'default'`). First literacy adopter of `diModeContract`. 6 drives PASS, 0 HIGH; the live probe renamed the deletion mode and bought a positive compound oracle. Owed: mic row for `short_spoken_word` on multisyllable answers, `/curriculum-fit`. **18 TEST/ADAPTER GAP CLOSED 09-12** — `base-ten-blocks` shipped its judged loop with zero tests and no `DI_PORTS` adapter, so `--di` could not reach it at all. Now: `baseTenAdapter`, 99 tests across 4 suites, live probe, `qa/base-ten-blocks-di/`. Three drives PASS (read 8/8 · regroup 8/8 incl. 4 hands · **signature 10/10**, mirror-image pair both directions). Four defects found by the drives and fixed: an ask that stated its own answer on an empty receiving place, verdict lines invisible to `SPOKEN_SPAN_RE`, count-of-one agreement, a mode-blind sign-off. Mic **#155**. QUEUED: the same span defect in `balanceWorkshopScript` + `numberBondModes` + `numberBondSplit` (number-bond's drive plan reads `correctionLine` as the ask TODAY — verified), executor `/eval-fix`. **Next = `gas-laws-simulator` · `ph-explorer`.** | 09-12 |
| ↳ 🧮 **DI for older learners** | **3 of 7 SHIPPED**, uncommitted | Items **37** `di-worked-procedure` · **38** `di-deduction` · **39** `di-word-problem-setup` (09-10, first HANDS+VOICE math pack; bench zero false affirms; mic **#149**; **G3 = curriculum gap**, user ruling owed). Evidence in `qa/di/BACKLOG.md`. **Next = the three class sittings, then `di-mental-chain`.** | 09-10 |
| ? **math ports** | same stream | `fraction-circles[touch_fraction]` implemented 09-12: 27 frontend + 3 backend tests, typecheck 0, five generated scenarios and 5-item Live drive PASS; tablet/audio #153 ([report](my-tutoring-app/qa/fraction-touch/README.md)). `number-sequencer` DI implemented 09-11 (six modes; mic #150/#63). Evidence: item 18 and `qa/number-sequencer-di/REPORT.md`. `3d-shape-explorer` mic #131; `place-value-chart` #113/#63; `sorting-station` post-port reader-fit still owed. | 09-11 |
| ↳ **picture-vocabulary** | ✅ **g7+g8 PASS — item 26 CLOSED 09-02** | `same-category` **0/8 → 7/7 hard**, 46/48, zero false affirms/refusals. Took **4 levers not 3**; blunt precedence REJECTED (refused the generated partner). Item 27 confirmed live — `--di-bench-item` runs go **in parallel** (~6h → ~1.5h). Only **mic #118** left. **TU-6 fired live, INTERMITTENT.** | 09-02 |
| 📊 **Coverage campaign (200)** | **ACTIVE** | `fast-fact` L1 **CLOSED 09-02**: FF-4 + FF-5 struck. The `apply` bleed was not in the mode docs — an unconditional block handed back `'recall'` six lines under "generate ONLY apply". Prompt-only fix, 110 challenges clean. [report](my-tutoring-app/qa/eval-reports/fast-fact-2026-09-02.md). **Pull = the phase-enum trio.** | 09-02 |
| **Curriculum fit atlas** | **ACTIVE — refreshed** | 12 themes, **4 built**: `letter-workshop`, `you-and-me`, `story-bridge`, `story-ribbon` L0–L5. DSP-4, DSP-5, and PV-3 **CLOSED 09-09**. PV-3: shared picture gate; 2 association + 5 sibling runs pass. Pull: `k-grammar-completion` via `/add-eval-modes`. Human acceptance #143–#146. | 09-09 |
| **`reading-repair-studio`** | **BORN, L3 next** | G2 cold-reading self-correction (`notice_and_repair`, β 4.5). L0 practice + L1 eval-modes (47 tests, 7 live routing) + L2 tutoring + L5 sound all pass; `ownsOpening`/ `[READING_HELP]` suppress the transport greeting so the tutor never narrates checking state. Provisional local evidence only — no adaptive scoring pending child-voice calibration. Next: L3 support tiers, then L4 structural difficulty. [Reports](qa/eval-reports/reading-repair-studio-2026-09-13.md). | 09-13 |
| **K Math fit atlas** | **ACTIVE** — slice 3 closed, slice 7 part-closed | Slices 1-6 CLOSED 09-08. **CB-5 CLOSED 09-09**: compare_groups pools inside the range the objective names (shared `resolveScopeRange`, band = outer ceiling); A/MEAS draws 1-5, B draws 6-10, 5/5 distinct with an equal case. **Slice 7, 09-09**: 26 rows unblocked — bar-model K one-to-one (4 modes, 9 rows), counting-board counting-out family + count_on covered at K (5 modes, 5 rows), **measure-lab BORN** (pan balance + pour-to-fill, 4 modes, 4 rows), analog-clock K parts (3 modes, 3 rows), length-lab estimate/two-unit + body-part units (2 modes, 3 rows), shape-sorter real-object stimulus (2 rows), and both no-code catalog refreshes. Every mode probed live through its oracle and driven in headless Chrome. **Slice 3 CLOSED 09-09**: strategy-picker now resolves the objective number window (K "within 10" → maxNumber 10, sums 6-10 in both guided draws; control still 5); the resolver is shared with addition-subtraction-scene at `service/objectiveNumberWindow.ts`, probe 12/12. Residual SPK-2 in EVAL_TRACKER: the K strategy MENU still withholds make-ten and doubles. **`k-band-floor-reaudit` CLOSED 09-09** — the two floors that HELD on 09-08 now have K-reachable modes instead of a re-audit, and both redirects the review had named were probed first and neither carried its objective: `missing_part` gave ten unknown-addend turns with no subtraction in them, and `analog-clock / read` has no sequence while `sequence-5` bans clock times at K by design. Built `number-bond / related_fact` (β 3.0, spoken: one bond, two judged turns, and turn 2's known part is the number the child produced on turn 1 — so a symmetric bond is DROPPED, both turns would otherwise share an answer) and `time-sequencer / clock-sequence` (β 3.5, K: order activity cards each carrying an analog FACE at a whole hour; four code-owned drop gates because the face is the only cue and a cue that lies is worse than none). 18/18 + 8/8 draws, 9/9 Chrome drive, 3 rows partial → candidate (atlas 137 → 140). [report](my-tutoring-app/qa/eval-reports/k-held-floors-2026-09-09.md). Owed: a live mic drive of the bond loop and `/eval-test` on both modes. **`sorting-station-k-draw-quality` CLOSED 09-09**: honours `challengeCount`, variety rounds past 3 come from another set (not a forced axis), count+2 generated so the speakability drop gate trims overage not the lesson, and a label/picture agreement gate now guards the sort family + `two_attributes` (which had none). 2 K draws/mode at count=5 → 5/5; 14/14 unit; contract R11+R12. REMAINING slice 7: `pattern-k-gaps` (code half), `calendar-days-k`, `count-to-100-k`, `picture-thermometer`, `physical-observation`. | 09-09 |
| **Lesson Bench** | **PAUSED by user request** | Manifest self-evaluation, coverage-judge iteration, and synthetic lesson journeys paused. `/lesson-coverage` and `/lesson-journey` retired. Evidence and residuals retained in `qa/lesson-bench/BACKLOG.md`; no automatic pulls or re-judge gates. Independent primitive work continues in its owning queue. | 09-07 |
| 🏛️ **History suite (C3)** | **ACTIVE** — item 29 | `cause-effect-chain` **PORT 25 SHIPPED 09-03**: yes/no per card (spoken), chain built by HANDS on a stillness close, root pick spoken. Drives identify 10/10 · hands 5/5 · pick 5/5; tsc at baseline; mic **#130**. `era-explorer` port 24 (mic **#127**). Queued: G5 3-card chains (`/eval-fix`). **Next birth: `source-detective`.** | 09-03 |
| 🗣️ **TU-6 — tutor speaks state** | **unowned** — evidence in di item 32 | **CLASS, not a primitive bug.** `[CURRENT STATE]` read aloud 3/3 + 3/3 (08-23). **09-04, lesson END: the tutor FABRICATED a `[PRIMITIVE SWITCH]` and read it with NO text sent** — phantom mic turns + a switch held forever. Prompt-only bans lose. **Needs a non-voiceable channel + di item 32 (a).** | 09-04 |
| 🎙️ **Human-check queue** | **99 open** — only the user closes these | Mic **#100**–**#108**, **#110**–**#118**, **#123**, **#126**–**#131**, **#133**, **#134**, **#137 NEW** (`compare_choice` menu). Non-mic **#109**, **#119**, **#120**, **#122**, **#124**, **#125**, **#132**, **#135**, **#136** (a RULING). **#121** part-superseded by #127. **#63** BLOCKS code; **#90**. Next **#138**. | 09-06 |
| 0. **Science depth** | PARKED (was ACTIVE) | CELL-1 ✅ 08-18. Resume top = **LCS-1**, `/oracle-test` first. DNA-1 ✅ CB-1 ✅; #80 open. `qa/science-depth/`. | 08-18 |
| 1. **Reader-fit sweep** | PARKED — ✅ **UNBLOCKED 08-22** | Item 17's gate (**#77**) is VOID: the 08-19 DI port made all 5 solar-system modes spoken, so the tap-answer template it protected is gone. **The 08-08 template is superseded — do not copy it.** Executor `/add-eval-modes`, 3 primitives, an eval-hook decision. | 08-22 |
| 🎛️ **Live model (`gemini-3.8-live`)** | **DEFAULT since 09-15** (user ruling) | Wins continuity + tool honesty; DI bench 0/56 false affirms. Watch: empty-script cue silent 4/6 (di **43a**). SILENT-channel design ruling owed (**43b**). Rollback = `LUMINA_LIVE_MODEL`. | 09-15 |
| **DI closeout (CTX-2)** | Docs + 1 probe | The excavators run carries the post-fix floor-gate numbers; write the report citing it. Unproven: the **`wedged` watchdog** — a wedged-0 run cannot show it fires. item 15. | 08-10 |
| **`di-spoken-practice`** | SHIPPED, 5 modes | `71cba07` + `ead9ae1` + compare_choice `a5eb7ba` on `main`; **`explain_concept` UNCOMMITTED 09-07** (di item 36). Now DI-drivable (`--di`/`--di-bench`). Open: item 17 (embedded insets), the routing hold, fresh-draw yield (di 36 (1)). | 09-07 |
| 00. **Lesson ordering** | ⛔ **PARKED — top item REJECTED** | B′ killed 08-10 (`32267345`); arms B and C also rejected. **No ordering work left.** Residual is per-primitive content → `/primitive-contract`. | 08-18 |
| **Silent generator fallbacks** | DEMOTED, not a sweep | 33 generators fall back with no retry and no warn; 32 are math. Math DI ports force the same live probe, so it is a checklist a port consults. List in this row's git history. | 08-16 |
| **Support tiers (non-math) · LA K-2 grammar** | PARKED | Batch-3 needs evidence via `/eval-test`; grammar is BLOCKED on a user design ruling. | 08-05 |
| ⚠️ **IMG-1 — tutor blind to images** | PARKED (user: do not push forward) | If resumed, the cheap pedagogy half first: the tutor SAYING it cannot see instead of confabulating. `qa/tutor-reports/lesson-live-2026-08-10-excavators.md`. | 08-10 |
| 🤖 **Pip surface rollout** | **ACTIVE** — rulings owed | 184 of 212 catalog primitives publish a surface (batches 5–33 closed 09-15, report `qa/pip-surface/batch-5-33-2026-09-15.md`). Left: DECIDE (4 sims) and DESIGN (deep-dive, passage-studio per-block) need user rulings; BLOCKED safety-lab (SAFE-1), formula-card (modal), media-player (rebuild); HOLD 2 for `/curriculum-fit`; 17 perch. 55 existing-primitive bugs queued in EVAL_TRACKER for `/eval-fix`. Queue: `my-tutoring-app/qa/pip-surface/ROLLOUT.md`. | 09-15 |
| **Closed, no code owed** | 4 filed | `multiplication-explorer` ✅ `927b754` (gate #90). `Pip` ✅ `997c875`, UNFILED. **Intent contract 174/174 green 08-21** — `sentence-analyzer` was the last generator on the legacy signature; migrated context-native, runtime-probed. `npm test` exits 1 with 0 failing tests (`canvas-confetti` rAF) — rides the next `solar-system-explorer` touch. | 08-21 |
| **Delegated lane** | NONE | — | 08-16 |

## Recorded once, so it is not re-discovered as new

- **Deploy caveats.** `my-tutoring-app/vercel.json` pins **no branch**, so which branch
  production tracks is unconfirmed. `backend/cloudbuild.yaml` deploys Cloud Run
  **`ai-tutor-backend`** (us-east5); the live service is **`mountamo-education`** — the
  backend has not moved. Verified in-file 08-18.
- **`parent/link-student` has no verification** — a literal `# TODO`. NOT a row: the parent
  portal is vestigial and pre-Lumina (user ruling 08-14).
- **Pilot onboarding CLOSED 08-14** (user: onboarding is done). Queue deleted, items 1-3
  retired unbuilt on purpose.

## Standing hygiene

- **Strike the row in the same slice as the event.** The `main` row sold three days of
  phantom ship debt because the fast-forward happened and nothing came back here.
- **Re-grep IDs immediately before filing** — both HUMAN-CHECKS rows (`### #N`, newest at
  the TOP, one list) and queue items (`^### [0-9]`). Concurrent sessions are normal and IDs
  move; 08-21 found `qa/di/BACKLOG.md` carrying two item 25s filed a day apart.
- **A stale doctrine line costs more than a stale status line.** A rule (*"class X is
  BLOCKED"*) gets copied forward and silently changes what is built. When a ruling
  overturns one, grep its prose copies in queues, scripts and docblocks — `tsc` sees none.
- **Commit at the mechanism boundary** — in a shared lane, while you still know which lines
  are yours. Shared files (EVAL_TRACKER, BACKLOG, `run_tutor_live.py`) get their own slice.
- **Every closing session updates the owning queue AND this file's "As of" in the same
  slice**, routing findings per the table above rather than into this file.
