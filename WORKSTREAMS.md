# Workstreams — Portfolio Index

Start here for "what's next?", then pull the top item of an ACTIVE stream's queue.
**Queues are authority over this file; this file over memory.**

## ⚖️ Rules

This file holds **state** — not findings, evidence, or reasoning. Run the three gates
before you finish:

```bash
wc -c < WORKSTREAMS.md                                            # ≤ 10000 BYTES
awk 'length>400 && /^\|/ {print FNR": "length}' WORKSTREAMS.md    # empty (whole LINE, bytes)
grep -c '^> ### ' WORKSTREAMS.md                                  # 0 — /pm edits rows, never appends
```

It was split three times in four days and regrew each time — because findings were written
here instead of where their reader acts. Decide the home at WRITE time:

| The finding… | Home |
|---|---|
| changes how the next slice is **DONE** | the executor skill (`/add-di-loop`, `/reader-fit`, …) |
| is true about **ONE primitive** | `docs/contracts/<id>.md` |
| is a **defect CLASS** | its own queue item |
| is **evidence of a run** | `qa/tutor-reports/`, `qa/<lane>/` |
| is **state** (lane, health, next pull) | **here — as a row** |

**States:** ACTIVE = worked now · DELEGATED = another session holds it, read its report
first · PARKED = idle, queue trusted only as of its date · BLOCKED = named dependency.
Prior snapshots and the reasoning behind past calls: `git log -p WORKSTREAMS.md`.

## Portfolio — as of 2026-09-04

**WIP 2 ACTIVE + 1.** Judged-loop ports are ONE stream (`/add-di-loop`, queue
`qa/di/BACKLOG.md`); science / math / picture-vocabulary are its items, not rival lanes.

| Lane | State | Pull now | As of |
|---|---|---|---|
| ↳ **Place-value misconception pilot** | IN PROGRESS: shared LLM applicability verified | Shared LLM applicability replaces generation regex. Place Value first, then Blocks: 14 real positive/paraphrase/abstain cases passed; fresh authenticated one-source/two-primitive run passed. Both emit shared response evidence; real LLM/auth/store/generation harness passes for two draws each, cleanup verified. Review embeds contrasts. Next: live teaching/fading acceptance, general context delivery and canonical retry identity. No cross-primitive resolution; #113/#63 open. Review now carries station-by-station build plans for the first six catalog entries (`artifacts/math-pedagogy-review/plans.json`; fraction-bar SHIPPED 09-13 as a `build` consumer — identify cannot host the move — plus the shared source-agnostic delivery path and a signing-key fix; [report](my-tutoring-app/qa/misconception/fraction-bar-2026-09-13.md); bar-model and number-line in flight in parallel sessions). **09-13 fraction-circles compare IMPLEMENTED** (capture repair via `firstResponseScore` — compare always scored 100 — plus consumer `contrast_same_numerator_denominators` at five reviewed G3/G4 subskills; distiller, planner 27/27, authenticated PASS; queued in item 18: Grade 3 denominator scope `/topic-fidelity`, touch capture unprobed; #158) — [report](my-tutoring-app/qa/misconception/fraction-circles-2026-09-13.md). **09-13 bar-model picture_graph IMPLEMENTED** (capture repair + consumer, Grade 3 MEAS003-04-a; 14/14 real draws + authenticated PASS; residual axis-leak/variety in BACKLOG item 18) — [report](my-tutoring-app/qa/misconception/bar-model-picture-graph-2026-09-13.md). **09-13 base-ten-blocks read_blocks is now an observation SOURCE** (catalog skill scope, factual correction evidence, `SERVER_DELIVERED_SOURCES` capture/resolution policy, shared delivery, code-owned grade band; distiller 5/5, planner 12/12, authenticated PASS; mic #157; first-response gate adopted; queued in item 18: USER RULING on chart receiving blocks-origin observations) — [report](my-tutoring-app/qa/misconception/base-ten-blocks-origin-2026-09-13.md). **09-13 number-line jump IMPLEMENTED** (capture repair: exact-landing grading, the shared opt-in `firstResponseScore` gate now used by four consumers, hint leak NL-3 fixed; consumer `contrast_start_positions` at Grade 1 OPS001-03-a/OPS001-04-a; distiller, planner 6/6, generation 41/41, authenticated PASS; queued in item 18: NL-2 plot tolerance `/eval-fix`, `/misconception-test`; #159) — [report](my-tutoring-app/qa/misconception/number-line-jump-2026-09-13.md). **09-13 backend primitive-agnostic (USER RULING)**: no primitive names or tables in production backend code; catalog `observationDelivery: 'server'` relayed on capture, server delivery = stamped `scope_context` record property, generic receipts judge against the plan's `accepted_answers`; 76/76 misconception tests, full suite unchanged vs HEAD, typecheck 0, authenticated probe PASS. **09-13 frontend half CLOSED**: catalog `learningObservations: { eligible, retest? }` declaration + one generic branch in `geminiService.ts`; scope subject from `config.objectiveSubject`; place-value retest is a catalog `retest` declaration; typecheck 0, vitest 95/95, authenticated probes PASS with cleanup (fraction-bar, place-value, number-line) — [report](my-tutoring-app/qa/misconception/catalog-observation-consumers-2026-09-13.md). **09-13 area-model IMPLEMENTED** (capture repair — one wrong cell per model still scored 100 — plus consumer via the catalog declaration: `contrast_same_fact_across_places` for build_model/find_area/multiply, `contrast_equal_area_perimeters` for perimeter; distiller 4/4, planner 10/10, generation 30/31 (Gemini 503), authenticated PASS; queued in item 18: AM-3/4/5 `/eval-fix`, `/misconception-test`, `/topic-fidelity` one-digit homes; #160) — [report](my-tutoring-app/qa/misconception/area-model-2026-09-13.md). **09-14 number-tracer sequence IMPLEMENTED** (grader repaired first via `/eval-fix`: blind vision, geometry accepting wrong numerals, answer guide painted, repeated answers, runaway window call; consumer `contrast_gap_positions_in_one_run` at K/G1; distiller 5/5, planner 14/14, generation 24/24, authenticated PASS; #161; queued in item 18: NT-9..NT-12, `/misconception-test`) — [report](my-tutoring-app/qa/misconception/number-tracer-2026-09-14.md). **09-14 counting-board take_away/add_more/count_on IMPLEMENTED** (capture repair: boards misdescribed as "count N", `firstResponseScore` — three wrong first answers still scored 80; moves `contrast_same_start_different_change` + `count_on_exactly_one_more`; distiller 8/8, planner 24/24, generation 67/67, signed-packet replay PASS; #163; queued in item 18: `/misconception-test`; CNB-2/CNB-3 RESOLVED 09-14 via `/eval-fix` (compare larger group on either side, catalog eval modes submitted), compare move now buildable, CNB-4/CNB-5 filed) — [report](my-tutoring-app/qa/misconception/counting-board-2026-09-14.md). **09-14 ten-frame operate IMPLEMENTED** (capture repair on all eight types: voice items misdescribed, `firstResponseScore` — three wrong first answers still scored 80, evalMode `add`/`split` → catalog `operate`/`decompose` (TF-6); move `contrast_same_first_number_different_second` K–2; judge contradiction on subtraction doubles fixed, keep-doubles question TF-5 `/eval-fix`; distiller 8/8, planner 20/20, generation 33/33, signed-packet replay PASS; #164; queued in item 18: `/misconception-test`) — [report](my-tutoring-app/qa/misconception/ten-frame-2026-09-14.md). **09-14 judged evidence RUNNER-OWNED (handoff slice 1 DONE)**: `hooks/judgedRunEvidence.ts` sets `firstResponseScore` and keeps every item's first wrong attempt for all 58 runner components; counting-board/ten-frame builders + base-ten patch deleted, six more callers submit; census over 23 declared sources (real generation → mounted pack → real distiller): gate fires 20/20, 14 hypotheses name the signature miss, 4 capture gaps queued (ordinal-line, decodable-reader, 3d-shape-explorer, picture-vocabulary) — [report](my-tutoring-app/qa/misconception/judged-evidence-census-2026-09-14.md). **09-15 handoff slices 2–4 DONE**: one pack `observation` callback (four packs migrated, 71 on a deprecated alias), catalog eval mode normalised at the evaluation boundary (`evalModeKey.ts`, table-tested over the catalog), one generator adaptation step (`adaptationStep.ts`, all ten consumers ported, fences unchanged, ten-frame signed-packet replay 2/2) — [report](my-tutoring-app/qa/misconception/judged-evidence-slices-2-4-2026-09-15.md). **09-15 capture repairs DONE** for the four census abstains (line order, story sentence, solid geometry, picture named; all four on the one `observation` callback): ordinal-line now distils "counts from the back of the line", the other three abstain on the harness fixture, not on missing facts — [report](my-tutoring-app/qa/misconception/judged-capture-repairs-2026-09-15.md). **09-15 alias migration Phase A (math) DONE**: 11 packs on `observation`, stimulus stated, and `learningResponses` now actually submitted (the runner collected them but most hosts never put them in student work); census no regressions vs run2 — [report](my-tutoring-app/qa/misconception/observation-alias-phase-a-math-2026-09-15.md). **09-15 Phase B (literacy) DONE**: 18 packs, 7/7 census hypotheses hold, letter-spotter confusion pairs now corrections-only — [report](my-tutoring-app/qa/misconception/observation-alias-phase-b-literacy-2026-09-15.md). **09-15 Phase C (DI) DONE**: 5 packs, 4/4 census hypotheses hold, word-problem placements fully described — [report](my-tutoring-app/qa/misconception/observation-alias-phase-c-di-2026-09-15.md). **09-15 Phase D (other) DONE**: last 11 packs, no pack left on the alias; gesture observations now record the move — [report](my-tutoring-app/qa/misconception/observation-alias-phase-d-other-2026-09-15.md). **Next in this lane:** Phase E, delete the alias, full census rerun ([handoff](my-tutoring-app/qa/HANDOFF-observation-alias-migration-2026-09-15.md)); A–D uncommitted. **09-14 harness handoff revised**: no-login delivery replay piloted, per-primitive authenticated probes to be retired ([handoff](my-tutoring-app/qa/HANDOFF-misconception-probe-harness-2026-09-13.md)). **09-14 delivery = SIGNED LAUNCH PACKET (user direction: frontend pipeline, backend storage-only)**: `/generation-context` signs the learner's observations once; generation makes NO backend call; two signed read routes + six per-primitive authenticated probes deleted; replay needs no login and no backend; generic smoke PASS; #162 — [report](my-tutoring-app/qa/misconception/launch-packet-delivery-2026-09-14.md). Still queued in item 18: the primitive-specific backend modules (IRT prior registry etc.) as separate slices; USER RULING on blocks-origin → chart; USER RULING on moving retest receipt issuance off the generation path; shared probe harness runner/fixtures ([handoff](my-tutoring-app/qa/HANDOFF-misconception-probe-harness-2026-09-13.md)). [Current handoff](my-tutoring-app/qa/HANDOFF-learning-observations-2026-09-12.md). | 09-15 |
| 🚀 **PROD / `main`** | ✅ **LEVEL at `2ec88137`** | Shipped 09-13 in **4 slices**: learning-observation framework + 6-primitive sweep, `reading-repair-studio` L0-L2+L5 birth, a small auth-guard follow-up, `curriculum-design-review` skill. Gates: typecheck:lumina 0, frontend vitest 6132/0, backend pytest 65/65 in touched files. **Owed: mic sittings #125–#130** from the 09-04 ship — `di-dice-roll` reached `main` with NO live drive. | 09-13 |
| 🔝 **JUDGED-LOOP PORTS** | **ACTIVE** — `qa/di/BACKLOG.md` | Shipped: 18 math port, 31, **34**, **36 ✅ 09-07** (`explain_concept` — `concept_statement` BENCHED 0/32 false affirms, paraphrase 8/8; 6/6 fresh explain objectives `SUFFICIENT` when the block ships; `DI_PORTS` adapter closes #137's no-drive; mic #139; residual = fresh-draw yield ~60%, queued). Open: **33** `poetry-lab[rhyme_hunt]`; 30 owns 2 cap WARNs (+ a third witness: the ten-rod sitting's off-script drift). **35 RETRACTED**. **40 ✅ 09-12** — `syllable-clapper` three ACTS (`blend_syllables` β1.5 · `count_parts` β2.5 · `delete_compound` β3.5) where there were three word LENGTHS; length moved to the tier, `metrics.evalMode` fixed (attempts had all filed as `'default'`). First literacy adopter of `diModeContract`. 6 drives PASS, 0 HIGH; the live probe renamed the deletion mode and bought a positive compound oracle. Owed: mic row for `short_spoken_word` on multisyllable answers, `/curriculum-fit`. **18 TEST/ADAPTER GAP CLOSED 09-12** — `base-ten-blocks` shipped its judged loop with zero tests and no `DI_PORTS` adapter, so `--di` could not reach it at all. Now: `baseTenAdapter`, 99 tests across 4 suites, live probe, `qa/base-ten-blocks-di/`. Three drives PASS (read 8/8 · regroup 8/8 incl. 4 hands · **signature 10/10**, mirror-image pair both directions). Four defects found by the drives and fixed: an ask that stated its own answer on an empty receiving place, verdict lines invisible to `SPOKEN_SPAN_RE`, count-of-one agreement, a mode-blind sign-off. Mic **#155**. QUEUED: the same span defect in `balanceWorkshopScript` + `numberBondModes` + `numberBondSplit` (number-bond's drive plan reads `correctionLine` as the ask TODAY — verified), executor `/eval-fix`. **Next = `gas-laws-simulator` · `ph-explorer`.** | 09-12 |
| 🧩 **Third branch (judged loop)** | **VOICE HALF BUILT 09-19** — di item 44 | A judged turn may be a QUESTION, not only right/wrong. Opt-in `withHelpBranch()`; counting-board's 7 voice kinds piloted; 10 consumers inert. lumina 0, tsc 770, vitest 6902/0. **Unverified — mic #168.** Gesture half open. | 09-19 |
| ↳ 🧮 **DI for older learners** | **3 of 7 SHIPPED**, uncommitted | Items **37** `di-worked-procedure` · **38** `di-deduction` · **39** `di-word-problem-setup` (09-10, first HANDS+VOICE math pack; bench zero false affirms; mic **#149**; **G3 = curriculum gap**, user ruling owed). Evidence in `qa/di/BACKLOG.md`. **Next = the three class sittings, then `di-mental-chain`.** | 09-10 |
| ? **math ports** | same stream | `fraction-circles[touch_fraction]` implemented 09-12: 27 frontend + 3 backend tests, typecheck 0, five generated scenarios and 5-item Live drive PASS; tablet/audio #153 ([report](my-tutoring-app/qa/fraction-touch/README.md)). `number-sequencer` DI implemented 09-11 (six modes; mic #150/#63). Evidence: item 18 and `qa/number-sequencer-di/REPORT.md`. `3d-shape-explorer` mic #131; `place-value-chart` #113/#63; `sorting-station` post-port reader-fit still owed. | 09-11 |
| ↳ **picture-vocabulary** | ✅ **g7+g8 PASS — item 26 CLOSED 09-02** | `same-category` **0/8 → 7/7 hard**, 46/48, zero false affirms/refusals. Took **4 levers not 3**; blunt precedence REJECTED (refused the generated partner). Item 27 confirmed live — `--di-bench-item` runs go **in parallel** (~6h → ~1.5h). Only **mic #118** left. **TU-6 fired live, INTERMITTENT.** | 09-02 |
| 📊 **Coverage campaign (200)** | **ACTIVE** | `fast-fact` L1 **CLOSED 09-02**: FF-4 + FF-5 struck. The `apply` bleed was not in the mode docs — an unconditional block handed back `'recall'` six lines under "generate ONLY apply". Prompt-only fix, 110 challenges clean. [report](my-tutoring-app/qa/eval-reports/fast-fact-2026-09-02.md). **Pull = the phase-enum trio.** | 09-02 |
| **Curriculum fit atlas** | **ACTIVE — refreshed** | 12 themes, **4 built**: `letter-workshop`, `you-and-me`, `story-bridge`, `story-ribbon` L0–L5. DSP-4, DSP-5, and PV-3 **CLOSED 09-09**. PV-3: shared picture gate; 2 association + 5 sibling runs pass. Pull: `k-grammar-completion` via `/add-eval-modes`. Human acceptance #143–#146. | 09-09 |
| **`reading-repair-studio`** | **BORN, L3 next** | G2 cold-reading self-correction (`notice_and_repair`, β 4.5). L0 practice + L1 eval-modes (47 tests, 7 live routing) + L2 tutoring + L5 sound all pass; `ownsOpening`/ `[READING_HELP]` suppress the transport greeting so the tutor never narrates checking state. Provisional local evidence only — no adaptive scoring pending child-voice calibration. Next: L3 support tiers, then L4 structural difficulty. [Reports](qa/eval-reports/reading-repair-studio-2026-09-13.md). | 09-13 |
| **K Math fit atlas** | **ACTIVE** — slice 3 closed, slice 7 part-closed | Slices 1-6 CLOSED 09-08. **CB-5 CLOSED 09-09**: compare_groups pools inside the range the objective names (shared `resolveScopeRange`, band = outer ceiling); A/MEAS draws 1-5, B draws 6-10, 5/5 distinct with an equal case. **Slice 7, 09-09**: 26 rows unblocked — bar-model K one-to-one (4 modes, 9 rows), counting-board counting-out family + count_on covered at K (5 modes, 5 rows), **measure-lab BORN** (pan balance + pour-to-fill, 4 modes, 4 rows), analog-clock K parts (3 modes, 3 rows), length-lab estimate/two-unit + body-part units (2 modes, 3 rows), shape-sorter real-object stimulus (2 rows), and both no-code catalog refreshes. Every mode probed live through its oracle and driven in headless Chrome. **Slice 3 CLOSED 09-09**: strategy-picker now resolves the objective number window (K "within 10" → maxNumber 10, sums 6-10 in both guided draws; control still 5); the resolver is shared with addition-subtraction-scene at `service/objectiveNumberWindow.ts`, probe 12/12. Residual SPK-2 in EVAL_TRACKER: the K strategy MENU still withholds make-ten and doubles. **`k-band-floor-reaudit` CLOSED 09-09** — the two floors that HELD on 09-08 now have K-reachable modes instead of a re-audit, and both redirects the review had named were probed first and neither carried its objective: `missing_part` gave ten unknown-addend turns with no subtraction in them, and `analog-clock / read` has no sequence while `sequence-5` bans clock times at K by design. Built `number-bond / related_fact` (β 3.0, spoken: one bond, two judged turns, and turn 2's known part is the number the child produced on turn 1 — so a symmetric bond is DROPPED, both turns would otherwise share an answer) and `time-sequencer / clock-sequence` (β 3.5, K: order activity cards each carrying an analog FACE at a whole hour; four code-owned drop gates because the face is the only cue and a cue that lies is worse than none). 18/18 + 8/8 draws, 9/9 Chrome drive, 3 rows partial → candidate (atlas 137 → 140). [report](my-tutoring-app/qa/eval-reports/k-held-floors-2026-09-09.md). Owed: a live mic drive of the bond loop and `/eval-test` on both modes. **`sorting-station-k-draw-quality` CLOSED 09-09**: honours `challengeCount`, variety rounds past 3 come from another set (not a forced axis), count+2 generated so the speakability drop gate trims overage not the lesson, and a label/picture agreement gate now guards the sort family + `two_attributes` (which had none). 2 K draws/mode at count=5 → 5/5; 14/14 unit; contract R11+R12. REMAINING slice 7: `pattern-k-gaps` (code half), `calendar-days-k`, `count-to-100-k`, `picture-thermometer`, `physical-observation`. | 09-09 |
| **Lesson Bench** | **PAUSED by user request** | Manifest self-evaluation, coverage-judge iteration, and synthetic lesson journeys paused. `/lesson-coverage` and `/lesson-journey` retired. Evidence and residuals retained in `qa/lesson-bench/BACKLOG.md`; no automatic pulls or re-judge gates. Independent primitive work continues in its owning queue. | 09-07 |
| 🏛️ **History suite (C3)** | **ACTIVE** — item 29 | `cause-effect-chain` **PORT 25 SHIPPED 09-03**: yes/no per card (spoken), chain built by HANDS on a stillness close, root pick spoken. Drives identify 10/10 · hands 5/5 · pick 5/5; tsc at baseline; mic **#130**. `era-explorer` port 24 (mic **#127**). Queued: G5 3-card chains (`/eval-fix`). **Next birth: `source-detective`.** | 09-03 |
| 🗣️ **TU-6 — tutor speaks state** | **unowned** — evidence in di item 32 | **CLASS, not a primitive bug.** `[CURRENT STATE]` read aloud 3/3 + 3/3 (08-23). **09-04, lesson END: the tutor FABRICATED a `[PRIMITIVE SWITCH]` and read it with NO text sent** — phantom mic turns + a switch held forever. Prompt-only bans lose. **Needs a non-voiceable channel + di item 32 (a).** | 09-04 |
| 🎙️ **Human-check queue** | **99 open** — only the user closes these | Mic **#100**–**#108**, **#110**–**#118**, **#123**, **#126**–**#131**, **#133**, **#134**, **#137 NEW** (`compare_choice` menu). Non-mic **#109**, **#119**, **#120**, **#122**, **#124**, **#125**, **#132**, **#135**, **#136** (a RULING). **#121** part-superseded by #127. **#63** BLOCKS code; **#90**. Next **#138**. | 09-06 |
| 0. **Science depth** | PARKED (was ACTIVE) | CELL-1 ✅ 08-18. Resume top = **LCS-1**, `/oracle-test` first. DNA-1 ✅ CB-1 ✅; #80 open. `qa/science-depth/`. | 08-18 |
| 1. **Reader-fit sweep** | PARKED — ✅ **UNBLOCKED 08-22** | Item 17's gate (**#77**) is VOID: the 08-19 DI port made all 5 solar-system modes spoken, so the tap-answer template it protected is gone. **The 08-08 template is superseded — do not copy it.** Executor `/add-eval-modes`, 3 primitives, an eval-hook decision. | 08-22 |
| 🎛️ **Live model (`gemini-3.8-live`)** | **DEFAULT since 09-15** (user ruling) | Wins continuity + tool honesty; DI bench 0/56 false affirms. Watch: empty-script cue silent 4/6 (di **43a**). SILENT-channel design ruling owed (**43b**). Rollback = `LUMINA_LIVE_MODEL`. | 09-15 |
| **DI closeout (CTX-2)** | Docs + 1 probe | The excavators run carries the post-fix floor-gate numbers; write the report citing it. Unproven: the **`wedged` watchdog** — a wedged-0 run cannot show it fires. item 15. | 08-10 |
| **`di-spoken-practice`** | SHIPPED, 5 modes | `71cba07` + `ead9ae1` + compare_choice `a5eb7ba` on `main`; **`explain_concept` UNCOMMITTED 09-07** (di item 36). Now DI-drivable (`--di`/`--di-bench`). Open: item 17 (embedded insets), the routing hold, fresh-draw yield (di 36 (1)). | 09-07 |
| 00. **Lesson ordering** | ⛔ **PARKED — top item REJECTED** | B′ killed 08-10 (`32267345`); arms B and C also rejected. **No ordering work left.** Residual is per-primitive content → `/primitive-contract`. | 08-18 |
| **Silent generator fallbacks** | DEMOTED, not a sweep | 33 generators fall back with no retry and no warn; 32 are math. Math DI ports force the same live probe, so it is a checklist a port consults. List in this row's git history. | 08-16 |
| **Support tiers (non-math) · LA K-2 grammar** | PARKED | Batch-3 needs evidence via `/eval-test`; grammar is BLOCKED on a user design ruling. | 08-05 |
| ⚠️ **IMG-1 — tutor blind to images** | PARKED (user: do not push forward) | If resumed, the cheap pedagogy half first: the tutor SAYING it cannot see instead of confabulating. `qa/tutor-reports/lesson-live-2026-08-10-excavators.md`. | 08-10 |
| 🤖 **Pip surface rollout** | **ACTIVE** — rulings owed | 184 of 212 catalog primitives publish a surface (09-24: the five spoken DI packs regained Pip through one `DiTeachingStage` surface, REOPEN row closed) (batches 5–33 closed 09-15, report `qa/pip-surface/batch-5-33-2026-09-15.md`). Left: DECIDE (4 sims) and DESIGN (deep-dive, passage-studio per-block) need user rulings; BLOCKED safety-lab (SAFE-1), formula-card (modal), media-player (rebuild); HOLD 2 for `/curriculum-fit`; 17 perch. 55 existing-primitive bugs queued in EVAL_TRACKER for `/eval-fix`. Queue: `my-tutoring-app/qa/pip-surface/ROLLOUT.md`. | 09-15 |
| 🧭 **TypeSafe (System One) lane** | **MEASURED — 3 rulings owed** | Four code-scored benches done 09-15/16; ledger + rulings (a) flag-on quality study, (b) retrieval gate, (c) hypothesis verifier: `my-tutoring-app/qa/typesafe/README.md`. Shipped: opt-in flags `LUMINA_TYPESAFE_SUGGESTIONS` and `LUMINA_TYPESAFE_VERIFY` (off|shadow|gate; supported+leaks verifier on both distillers, 0/29 golden rejections), both default off with failsafe; ruling (d) = run shadow in real traffic. Verdict: verify-and-gate, never a ranker/distiller/planner replacement. | 09-16 |
| 🚛 **Student interests → themed lessons** | **ALL 3 SLICES + PreK objective cap SHIPPED** | Interests live on `students/{id}.interests` (merge write + `scripts/set_student_interests.py`), persona reads Firestore-first with the Cosmos fallback intact. `counting-board` renders a themed triple (emoji + plural + singular) with the six-emoji enum as the floor — contract R12. Verified end to end: interests → persona → manifest intent → 🚛 "dump trucks" ([trace](my-tutoring-app/qa/topic-traces/counting-board-student-interests-2026-09-16.json), 12/12 [probe](my-tutoring-app/scripts/probe-counting-board-themed-objects.mjs)). Objective cap 1 PreK / 2 K / 3 above in `objectiveBudget.ts` (brief trims in code; fill uses it); at one objective every component intent is themed ([evidence](my-tutoring-app/qa/topic-traces/objective-cap-by-band-2026-09-16.md): PreK 1 obj / 4 comps, was 3 / 10). Owed: HUMAN-CHECKS #166, confirm student 9001 = the pilot student. [handoff](my-tutoring-app/qa/HANDOFF-student-interests-themed-lessons-2026-09-16.md). [Rollout queue](my-tutoring-app/qa/personalization/ROLLOUT.md) **row 0 CLOSED 09-16**: a themed intent put "truck" in 4/4 K decodable passages and replaced word-sorter's named groups in 8/12. The shared `themedFocusLine` (theme → carrier, ≤2 everyday theme words) is now in 10 literacy generators, plus a decodable K re-draw gate and a phonics-blender CVC gate; after the fix both are at control level ([evidence](my-tutoring-app/qa/personalization/row0-literacy-themed-targets-2026-09-16.md)). Row 0b (picture-vocabulary) needs a user decision. **Next: row 1** addition-subtraction-scene, then row 2 comparison-builder (triple port). Audit filed MLB-1. | 09-16 |
| 🎙️ **Live tutor tools + lesson pilot** | **S2 PILOT WIRED** (09-19) | `/lumina/live-activity` confirms tool calls fire and primitives react. The original bench remains a tool lab; the S2 pilot now also wires the runtime through `LessonScreen` for two verified modes. Current: tutor/JEV workspace on Counting Board; latest user sitting completed seven items. User-selected second binding: Shape Sorter `identify`, using the same observer/runtime. Invariants and evidence: [teaching workspace](my-tutoring-app/src/components/lumina/docs/TEACHING_WORKSPACE.md), [second-adopter report](my-tutoring-app/qa/tutor-reports/shape-sorter-teaching-2026-09-19.md). User direction: sunset the scripted architecture. **LA-14 S0+S1 done 09-19** — [census](my-tutoring-app/qa/live-runtime-handoffs/07-census.md) has the per-surface state (20 learner surfaces still call a runner hook; the other nine live adapters never did), and both pilots now read their task from `countingBoardDomain.ts`/`shapeSorterDomain.ts` with `CountingController` declared natively instead of as a `JudgedScriptRun` facade. S2 now wires `count`/`identify` into normal Kindergarten and scroll lessons with existing evaluation submissions (user: tutor completion counts). [Wiring and verification](my-tutoring-app/qa/tutor-reports/lesson-workspace-wiring-2026-09-19.md). Other modes and runner deletion remain subsequent slices. [LA-14 handoff](my-tutoring-app/qa/live-runtime-handoffs/07-sunset-scripted-tutoring.md). **LA-13 completion-stall repair done 09-19:** both saved stalls were JEV choosing `correct` under threshold; the `correct` criterion read as requiring the answer to be restated, and Counting Board's `counted: 0` scene fact (objects marked, always zero for a spoken answer) contradicted the affirming tutor. Shared criteria repaired, `counted` → `markedOnBoard`, refused observations report no score, and a settled turn that records nothing says so once. 54/54 + 42/42 real JEV, 3/3 + 3/3 connected audio, 6975 frontend tests green (18 stale-mock failures the S2 slice left behind also fixed). [Report](my-tutoring-app/qa/tutor-reports/lesson-workspace-completion-repair-2026-09-19.md). **Third adopter done 09-19:** Number Train's five SPOKEN modes (`count_from`, `before_after`, `fill_missing`, `spot_error`, `decade_fill`) run on the shared workspace in the live host and in ordinary lessons; domain extracted S1-style; 42/42 new real JEV cases with shapes 42/42 and counting board 54/54 unchanged; 3/3 + 3/3 connected `--lesson-entry --audio`; 7018 frontend tests green. One shared `correct`-criterion sentence: a very short agreement naming the expected answer credits the learner. [Report](my-tutoring-app/qa/tutor-reports/number-sequencer-teaching-2026-09-19.md). **Next pull: the lesson shell has no learner-owned Try again / Next challenge**, so a checked-wrong manipulation locks the surface until an observer transition reopens it — that blocks EVERY gesture mode from lesson entry and is why `order_cards` is withheld (its binding and tests are built). Then LA-14 S3, retire one replaced pilot path. Shape substep abstention, the uncertified open-assignment cue, assistance bookkeeping, mic/visual #167 and planned orders remain open. **Learner signals + observation kinds: piloted 09-20 on Counting Board, then GENERAL for every shared-workspace binding the same day (user request; no flag, no per-primitive wiring, packet self-describes because two adopters' guidance is at the 2000-char cap; per-domain real JEV cases 0 false requests; Shape Sorter + Number Train `--audio` journeys PASS; `/add-live-tutor-tools` §2/§5 updated and dry-run verified).** Pilot detail: code-computed per-item facts ride the packet as `liveRuntime.learner`, the verdict trio is now the first registered JEV kind (54/54 real cases unchanged), and an advisory learner-turn kind classifies help / stop / answer-attempt without the expected answer (87/87 real cases, 0 false requests, median 213 ms). 3/3 connected `--audio` journeys PASS; 1784 frontend tests, typecheck 0. No timer speaks and no kind picks a hint. **Queued next in this stream:** behavioral comparison of the tutor with and without the packet on the roadmap replay sequences, executor `/tutor-test`; then widen the opt-in. [Report](my-tutoring-app/qa/tutor-reports/counting-board-learner-signals-2026-09-20.md). **Fourth adopter di-letter-sounds done 09-19; fifth adopter di-word-reading done 09-20 — and adoption should now PAUSE.** All four word-reading modes bind in the dev host (domain extracted S1-style; per-letter demonstration targets on a decodable word, none on an irregular sight word; the reward picture is not a workspace object and is revealed only on a committed read). 45/48 real JEV, 671 affected tests, typecheck:lumina 0, full tsc 770 unchanged. The audio gate RAN and passed the channel letter sounds could not: a synthesised whole word transcribes correctly and a connected run closed `dog` -> retry, `cat` -> success, `mat` -> success to completion. **But the sub-threshold-affirmation finding is NOT specific to produced sound:** a correct read affirmed with "You did it!" after a corrective turn classified `correct` at 0.86 against the 0.9 gate, the observer abstained, and the live lesson STALLED to timeout (`sight_word` 0/3 for this reason). Two success-condition clauses were tried in the primitive's own layer and both reverted — one had no effect, one only nudged a single case onto the threshold while costing certainty elsewhere. **Next pull in this stream is therefore LA-13's shared criterion, not a sixth adopter:** should a whole-assignment affirmation that names no answer be creditable when the prior tutor turn corrected or modelled the target? Five adopters, three domains, same 0.84-0.86 band. Two harness families are also past three occurrences and are shared-layer work: the retry-path timeout, and a requested demonstration narrated without being performed (text called `demonstrate` 3/3, audio 0/2). [Report](my-tutoring-app/qa/tutor-reports/di-word-reading-teaching-2026-09-20.md). **Sixth adopter di-math-facts done 09-20** (user: next math primitive; its answer is neither printed nor a produced phoneme, so it is the cheapest test of the LA-13 question). Five spoken modes bind in the dev host; 63/63 real JEV, 42/42 learner-intent 0 false requests, 12/19 connected journeys, typecheck:lumina 0. **LA-13 narrowed:** the sub-threshold shape does NOT reproduce on a spoken number, and a deterministic isolation shows naming the answer is not the remedy. [Report](my-tutoring-app/qa/tutor-reports/di-math-facts-teaching-2026-09-20.md). **Brief 10 DONE 09-20 + USER RULING (no existing mode is barred from lessons; fix the error instead).** The allowlist is gone; a family declares `bindsTeachingWorkspace` and every one of its modes binds in ordinary lessons — the three DI packs, `order_cards` and all ten counting-board modes. The gesture stall that justified the withholding was fixed: the learner's Try again / Next challenge moved from the dev sandbox into the shared shell `LiveRuntimeSurface`. One catalog-derived mode/content gate is shared with `livePlan.ts`; the duplicated evaluation-submit, scripted/teaching switch, adapter boilerplate, observer scope keys and routes are shared helpers. Seven findings that change tutor/JEV input are queued with executors in [brief 11](my-tutoring-app/qa/live-runtime-handoffs/11-structure-review-residuals.md). **Seventh adopter letter-sound-link done 09-20** (user: move to the main literacy primitives). First literacy binding outside the DI packs and first with TWO channels: `hear_see` is a TAP whose answer the tutor is never told and which offers no demonstration, because every object on its stage is an answer option. All three modes bind in lessons. 57/63 real JEV, 42/48 learner-intent 0 false requests, journeys see_hear 3/3 text + 1/3 audio, hear_see 3/3, keyword_match 2/3; typecheck:lumina 0, 7150 tests. **LA-13 narrowed again:** the produced-sound `wrong_then_corrected` shape PASSES here at 0.94-0.96, so the failing line is not the same-utterance-same-channel reading — a deterministic pair shows it is a reply whose entire content is the answer token ("Yes, sss." abstains; the sentence form scores 0.99-1.00). [Report](my-tutoring-app/qa/tutor-reports/letter-sound-link-teaching-2026-09-20.md). ~~Next pull is still LA-13's shared criterion~~ **Answered 09-21 on the tutor side (brief 11 item 4, user ruling: `/add-live-tutor-tools` edits existing spoken DI in place, the measurement decides).** One `WORKSPACE_DOCTRINE` in `adapterContract.ts` replaces the doctrine retyped across seven adapters; the credit rule is "credit the learner and name what they got right". 21 audio journeys: 40/42 correct answers credited, 0 observer abstentions on a crediting reply, 0 false credit, 21/21 demonstrations, 16/21 passed (13/21 before). The observer criterion was not changed. Moving the doctrine into the backend session instruction was measured and reverted (demonstrations 19→14/21). A refused workspace action now returns its reason; the journey harness answers every item instead of assuming a 2-item lesson. [Report](my-tutoring-app/qa/tutor-reports/workspace-doctrine-2026-09-21.md). **Brief 11 item 1 DONE 09-21:** host-written text is `author: 'host'`, never learner words ([report](my-tutoring-app/qa/tutor-reports/host-text-source-2026-09-21.md)). **Items 6 DONE + 3 NOT ADOPTED 09-21:** domains export `workspaceScene`, the verdict probe imports it; removing `markMeaning` cost demonstrations (3/15 vs 1/15), so it stays ([report](my-tutoring-app/qa/tutor-reports/workspace-scene-2026-09-21.md)). **Item 7 DONE 09-21:** `DiTeachingStage` shell, 9/9 audio journeys. Brief 11 has nothing left for this executor (2 → `/tutor-test`, 5 → `/add-voice-control`, 8 is a standing cap). **LA-14 S3 slice 1 DONE 09-21:** blended/`mixed` pins run on the workspace when every mode binds; number-sequencer's scripted live branch deleted; blends record as before, per-mode recording queued for `/student-data-loop` (user ruling: per item) ([report](my-tutoring-app/qa/tutor-reports/blend-pins-on-workspace-2026-09-21.md)). **LA-14 S3 slice 2 DONE 09-21:** Pulse hosts the workspace (shared `workspaceBinding`, item's `eval_mode_name` as the pin, a runtime + Live session per bound item, identical submissions per family); Practice deleted (user ruling: vestigial, ~6,000 lines) and the home screen's Practice mode now opens Pulse; Pulse's generator is pinned to the IRT-chosen mode (user ruling); Pulse's Language Arts/Reading subjects fixed. 12 host tests, 7153 tests green, tsc 770. A bound Pulse item still needs a browser sighting (#167). **Queued for `/student-data-loop` + user:** mastery-lifecycle `subject` values are stored in mixed casing and Pulse's exact filter sees 13 of 159 math lifecycles in a sample ([report](my-tutoring-app/qa/tutor-reports/pulse-on-workspace-2026-09-21.md)). **Eighth adopter di-sentence-reading BUILT 09-22** (user: literacy primitive), fourth DI pack, first connected-text pack on the workspace: domain extracted, all four modes bind, 26/26 deterministic tests, typecheck 0, tsc 770 unchanged. Real JEV/learner-intent probes and a connected `--audio` journey are UNRUN (deferred on user cost request) — not yet workspace VERIFIED. [Report](my-tutoring-app/qa/tutor-reports/di-sentence-reading-teaching-2026-09-22.md). **S4 Shape Sorter siblings DONE 09-22:** `count`/`sort`/`find_real_object` joined `identify` — every catalog mode binds, closing the last part-migrated primitive. 7193 tests green, typecheck 0, tsc 770 unchanged. 87/90 learner-intent (0 false requests), 92/96 JEV. Connected audio: `count` 1/1, `find_real_object` 1/1, `sort` 0/5 — every sort failure is a real correct credit the observer under-confidently refuses (LA-13 shape; new domain evidence, mats' labels double as natural sub-step vocabulary). [Report](my-tutoring-app/qa/tutor-reports/shape-sorter-siblings-teaching-2026-09-22.md). **LA-14 S5 DONE 09-22 ([handoff 13](my-tutoring-app/qa/live-runtime-handoffs/13-delete-di-scripted-drills.md)):** the scripted drills of di-letter-sounds, di-word-reading, di-math-facts and di-sentence-reading are deleted (4,572 production lines → 266; 1,819 drill-only test lines). An unbound mount shows a visible "needs the tutor" card; the DI tester mounts the packs in a one-item workspace host (screenshots from the real host). Step 0: the multi-objective blocker is unreachable (73 saved + 6 fresh lessons, 0 multi-objective content sections); the real unbind cause was a 12-item pool cap below letter sounds' 20-item review set, now per domain — 44/44 sections bind. Sentence reading verified at last: 3/3 `--lesson-entry --audio`, learner-intent 36/39 (0 false requests), JEV 26/36 (all misses under-credit, 0 over-credit). Pip for the four packs re-queued on `DiTeachingStage` (`/add-pip-surface`); math-facts misconception evidence and response timing on the workspace queued (`/add-misconception-loop`). [Report](my-tutoring-app/qa/tutor-reports/di-drill-deletion-2026-09-22.md). **LA-13 shared criterion DONE 09-22 ([handoff 14](my-tutoring-app/qa/live-runtime-handoffs/14-la13-crediting-reply-abstains.md)):** three sentences in the verdict criterion ("generic praise" no longer counts against praise that credits the answer; praise for accuracy/completeness credits; affirming the learner's answer credits even after the tutor modelled it). Verdict probe 445/443 -> 473/478 of 510 across eight domains, 0 false credit everywhere; two attempts reverted. Connected `--audio`: word 3/3, sentence 2/3 then 3/3, letter sounds 1/3 (one synthetic-audio), sort 1/3, counting board 3/3. Shape-sorter probe one case below baseline (`group`, at the gate). A tutor turn transcribed as `<no speech>{pause}` is no longer observed; it had reopened a correctly read item. User ran all four shape-sorter modes in the browser: worked. [Report](my-tutoring-app/qa/tutor-reports/la13-crediting-criterion-2026-09-22.md). **Next pull: [handoff 15](my-tutoring-app/qa/live-runtime-handoffs/15-workspace-rollout.md), the workspace rollout** (user direction 09-22: minimal wiring on every answerable primitive, queue like Pip's, custom scaffolding after). LA-13 follow-ups parked as W2. **Ten-frame W1 DONE 09-22:** all seven modes on the workspace in the live host and lessons; controller swapped at the component boundary, no Teaching copy; smoke `build` + `subitize --audio` PASS after fixing a retry that un-saw the flash, adding a learner **Show me**, and two shared harness defects (learner Try again press; `<no speech>` turns); 7127 tests, tsc 770. [Report](my-tutoring-app/qa/tutor-reports/ten-frame-w1-2026-09-22.md). **Simplified same day:** shared `useWorkspaceRunner`, catalog `teachingWorkspace` + `workspaceAdapter`, registry-driven renderers; all nine adopters moved, tutor input byte-identical. **Number-bond + compare-objects W1 DONE 09-23** (parallel sessions; number-bond ~5 min to clean typecheck on the new recipe; shared fix: no count fact beside a spoken answer) ([number-bond](my-tutoring-app/qa/tutor-reports/number-bond-w1-2026-09-23.md), [compare-objects](my-tutoring-app/qa/tutor-reports/compare-objects-w1-2026-09-22.md)). **Handoff 15 steps 3-5 DONE 09-23:** W1 recipe in `/add-live-tutor-tools`, dry-run twice; queue [`qa/workspace-rollout/ROLLOUT.md`](my-tutoring-app/qa/workspace-rollout/ROLLOUT.md) covers 212 ids; batch A1 (place-value-chart, ordinal-line, sorting-station) at W1, no live adapter is runner-owned any more ([report](my-tutoring-app/qa/tutor-reports/workspace-rollout-A1-2026-09-23.md)). Shared fix: a credit-and-advance verdict skipped every W1 success handler (number-bond lost evidence); harness text entry and retry-path timeout fixed. **DEAD row + A2 DONE 09-23:** plain-shape recipe (`useWorkspaceProgress`, the drop-in for `useChallengeProgress` that 74 primitives use), number-line/comparison-builder/number-tracer at W1; every live adapter is on the workspace; harness gained `draw` and stopped hanging on canvas primitives ([report](my-tutoring-app/qa/tutor-reports/workspace-rollout-A2-2026-09-23.md)). **B1 DONE 09-23:** balance-scale, base-ten-blocks, bar-model, fraction-circles at W1, every mode (9 surfaces: a family binds all its modes, so the plain components moved too); 7 smokes PASS after two ask fixes the observer exposed (bar-model asked for more than its check; base-ten asked a plural over one block) and one harness fix; tsc 770, 2156 tests. **Ruling owed:** balance-scale `two_step` explanation is now scored ([report](my-tutoring-app/qa/tutor-reports/workspace-rollout-B1-2026-09-23.md)). **B1 scripted paths DELETED 09-23 (user ruling: one path):** testers host bound families on the workspace (`TesterWorkspace`, a07392a7); an unpinned section binds as `mixed` (61688eef; saved lessons 158/159 bound, the miss is invalid content); bar-model pilot 1c82649c then balance-scale/base-ten/fraction-circles 22d40e6c (~-2,600 production+test lines), each workspace-only via `withWorkspaceOnly`, an unbound mount shows the shared NeedsTutor card; 8 smokes PASS. **Owed:** user rulings on the scratch pad (it mounts these four with no host, now the card) and on spoken-evidence capture (the runner was the only source of heard-answer evidence; base-ten's read_blocks lost it at 7a0ae9ce); balance-scale's catalog tutoring keys (`currentEquation`, `variableValue`) are now never filled (`/tutor-test`). **No dead end after an answer DONE 09-24 (user ruling, 132a8685):** below the verdict gate a finished reply to a spoken answer resolves (not credited -> retry; likely credit -> the host asks the tutor to say plainly, `confirm_credit`); 8-domain replay (510 real answers, `scripts/observer-rule-replay.mjs`) strandings 37 -> 0, false credit 0. **Scoring pass DONE 09-24 (user direction, 3a18ea0a):** the tutor's verdict moves the lesson; `item_score` re-grades each spoken attempt's own answer for the record (>=.8/<=.2, else the flow verdict stands); 24x3 real cases false credit 0; learner words now recorded apart from tutor replies. Open: bar-model's plain-shape spoken record still follows the flow; tutor spoke its planning text once (di-letter-sounds drive). **Generic W1 contract test DONE 09-24:** `runtime/workspaceContract.test.tsx` mounts all 21 bound families on 53 saved generated payloads (181 cases; a family with no payload fails); shared `mountWorkspace` harness; 144 duplicated lines out of the per-primitive tests; mutation-checked (a live legacy AI hook and a self-committing timer both fail it). **B2 DONE 09-24** (aa43415e, aa1d6129, 6750001f, 656131ba): the four speech-loop literacy families run on the workspace only, every mode; production +836/−2,176; 8 smokes PASS; shared: binding counts items from the family's mount state (phonics-blender's pool is `words`). ([report](my-tutoring-app/qa/tutor-reports/workspace-rollout-B2-2026-09-24.md)). **Harness resume fix DONE 09-24:** a drive records `session_resuming`/`session_resumed` as `provider_resume` and keeps driving; runs print `(N Live resume)`. Forced drop (`LUMINA_FAULT_DROP_S` on a separate :8010 backend): baseline FAIL, fixed PASS (1 Live resume). **Found and queued (backend, `/add-live-tutor-tools`, needs a live probe):** when the resume itself fails with 1011 the backend reconnects COLD, sending no `session_resumed` and no steering, so the tutor goes silent on a fresh session (reproduced once; a child would see a stalled lesson); and a mid-turn resume repeated the tutor's first words once. **B3 DONE 09-24** (98b2838a, ad1c5a26, 1c319412, a35c66f7): you-and-me, ramp-lab, di-shapes, spatial-scene on the workspace only, every mode; 8 smokes PASS; found and fixed: a scene fact read from controls reset after an advance supersedes the advance (derive facts in render, item-scoped). ([report](my-tutoring-app/qa/tutor-reports/workspace-rollout-B3-2026-09-24.md)). **C1 DONE 09-24** (ff355679, 1745179a, e8411569, 700cbd33): syllable-clapper, rhyme-studio, phoneme-explorer, word-workout on the workspace only, every mode; 8 smokes PASS (two on r2); rhyme-studio production/collection undriven (no code-owned answer); the tutor never answered `[LESSON_START]` in 2 of 10 drives with no resume involved, harness-only so far (user 09-24: not seen in live use; a harness note) ([report](my-tutoring-app/qa/tutor-reports/workspace-rollout-C1-2026-09-24.md)). **C2 DONE 09-24** (f005a642, 371578cc, 0230a58e, d53dc3d2): word-builder, word-sorter, picture-vocabulary, letter-spotter on the workspace only, every mode; 8 smokes PASS; word-builder guidance fixed (the tutor asked about one part before an attempt); one markdown "Answer Key" tutor output recorded, not patched ([report](my-tutoring-app/qa/tutor-reports/workspace-rollout-C2-2026-09-24.md)). Queued, not next: the cold-reconnect defect (backend `lumina_tutor.py`; harness-seen, not reported live). **C3 DONE 09-25** (762f476f, 2e6c6ce5, 84d01c56, 0f7f6907): decodable-reader, interactive-book, story-bridge, story-ribbon on the workspace only, every mode; 8 smokes PASS (one on r2). Shared fix 637f2553: a scene fact over 500 chars made the observer refuse every judgment on that item, silently; `sceneFacts.ts` splits long text and the generic W1 contract now validates every payload's observer request. Recorded: the tutor praised a true one-story detail ("That's right…") before asking for the comparison, and summarised the two stories instead of reading them ([report](my-tutoring-app/qa/tutor-reports/workspace-rollout-C3-2026-09-25.md)). **C4 DONE 09-25** (e200a155, ba74b08c, 72556db8, 68c70a35): addition-subtraction-scene, 3d-shape-explorer, calendar-explorer (grid plain + chain runner-era), push-pull-arena on the workspace only, every mode; 8 smokes PASS (one on r2, a harness fix); nine runner-mock/click-era suites moved onto the real runtime; push-pull predict/compare now reveal on credit; recorded: the tutor skipped reading the second riddle's clues (same shape as C3's summarised stories) ([report](my-tutoring-app/qa/tutor-reports/workspace-rollout-C4-2026-09-25.md)). **C5 DONE 09-26** (2e67363a, 5b5b5186, a7e4821d, 330ae84e): habitat-diorama, matter-explorer, states-of-matter, solar-system-explorer on the workspace only, every mode; each keeps its ungraded exploration face; 8 smokes PASS, all first run; tsc 770. Recorded: after a wrong answer the tutor often gives or nearly gives the answer, now in four families (C4's leading hint); whether that credit counts as independent is a `$student-data-loop` question ([report](my-tutoring-app/qa/tutor-reports/workspace-rollout-C5-2026-09-26.md)). **C6 DONE 09-26** (ca9f5ffe, bb380730, 48fbe404, 6e4c7015, 375b203b): the five older-learner DI packs on the workspace only, every mode (four on `DiTeachingStage`, word-problem's big-number step a checked gesture); 10 smokes PASS, 3 FAIL recorded; tsc 770. Fixed: worked-procedure's decide ask gave the decision away. Found: the observer refuses credit for a step of a larger problem (whole problem in the task; "…ready to subtract!"; "…in the big spot!"), two adopters, evidence in [handoff 14](my-tutoring-app/qa/live-runtime-handoffs/14-la13-crediting-reply-abstains.md) candidate 1; deduction cannot_tell draws one rule (`/eval-fix`) ([report](my-tutoring-app/qa/tutor-reports/workspace-rollout-C6-2026-09-26.md)). **Next pull: LA-13 step-credit criterion (handoff 14 candidate 1, eight-domain measurement), then batch C7.** | 09-26 |
| **Closed, no code owed** | 4 filed | `multiplication-explorer` ✅ `927b754` (gate #90). `Pip` ✅ `997c875`, UNFILED. **Intent contract 174/174 green 08-21** — `sentence-analyzer` was the last generator on the legacy signature; migrated context-native, runtime-probed. `npm test` exits 1 with 0 failing tests (`canvas-confetti` rAF) — rides the next `solar-system-explorer` touch. | 08-21 |
| **Delegated lane** | NONE | — | 08-16 |

## Recorded once, so it is not re-discovered as new

- **Deploy caveats.** `my-tutoring-app/vercel.json` pins **no branch**, so which branch
  production tracks is unconfirmed. `backend/cloudbuild.yaml` deploys Cloud Run
  **`ai-tutor-backend`** (us-east5); the live service is **`mountamo-education`** — the
  backend has not moved. Verified in-file 08-18.
- **`parent/link-student` has no verification** — a literal `# TODO`. NOT a row: the parent
  portal is vestigial and pre-Lumina (user ruling 08-14).
- **Pilot onboarding CLOSED 08-14** (user: onboarding is done). Queue deleted, items 1-3
  retired unbuilt on purpose.

## Standing hygiene

- **Strike the row in the same slice as the event.** The `main` row sold three days of
  phantom ship debt because the fast-forward happened and nothing came back here.
- **Re-grep IDs immediately before filing** — both HUMAN-CHECKS rows (`### #N`, newest at
  the TOP, one list) and queue items (`^### [0-9]`). Concurrent sessions are normal and IDs
  move; 08-21 found `qa/di/BACKLOG.md` carrying two item 25s filed a day apart.
- **A stale doctrine line costs more than a stale status line.** A rule (*"class X is
  BLOCKED"*) gets copied forward and silently changes what is built. When a ruling
  overturns one, grep its prose copies in queues, scripts and docblocks — `tsc` sees none.
- **Commit at the mechanism boundary** — in a shared lane, while you still know which lines
  are yours. Shared files (EVAL_TRACKER, BACKLOG, `run_tutor_live.py`) get their own slice.
- **Every closing session updates the owning queue AND this file's "As of" in the same
  slice**, routing findings per the table above rather than into this file.
